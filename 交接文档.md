# 短视频热点洞察与智能生产系统 - 项目交接文档

## 文档信息

- **项目名称**: 短视频热点洞察与智能生产系统
- **版本**: 2.2.0
- **最后更新**: 2026-07-20
- **维护团队**: Crow5 开发团队
- **文档目的**: 项目交接与新人引导

---

## 1. 项目概述

### 1.1 项目背景

本系统是面向品牌营销团队的短视频内容生产平台，旨在解决短视频内容创作效率低、多平台发布繁琐、热点追踪困难等问题。系统集成了热点发现、内容生产、智能剪辑、数字人生成和多平台发布等核心功能，实现从热点发现到内容发布的全流程自动化。

### 1.2 核心价值

- **效率提升**: 自动化热点发现与内容生产，减少人工操作
- **质量保证**: AI 驱动的内容生成与校对，确保内容质量
- **多平台覆盖**: 支持抖音、小红书、视频号等主流平台
- **数据驱动**: 基于热度数据的智能决策支持

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户界面层                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ React 前端  │  │   移动端    │                           │
│  │  (端口1001) │  │   (规划中)  │                           │
│  └──────┬──────┘  └──────┬──────┘                           │
│         │                │                                  │
│         └────────────────┘                                  │
│                          │                                  │
│                    ┌─────▼─────┐                           │
│                    │  API 网关  │                           │
│                    │ (端口2001) │                           │
│                    └─────┬─────┘                           │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                    业务逻辑层                                │
│  ┌─────────────┐  ┌─────▼─────┐  ┌─────────────┐         │
│  │  热点发现   │  │  内容生产  │  │  发布管理   │         │
│  │   服务      │  │   服务     │  │    服务     │         │
│  └─────────────┘  └───────────┘  └─────────────┘         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                    数据存储层                                │
│  ┌─────────────┐  ┌─────▼─────┐  ┌─────────────┐         │
│  │   SQLite    │  │ PostgreSQL│  │   文件存储   │         │
│  │  (开发环境) │  │ (生产环境) │  │   (媒体)    │         │
│  └─────────────┘  └───────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 核心功能清单

### 2.1 爆火视频检索

**功能描述**: 三平台（抖音、小红书、视频号）商业 API 统一网关，支持热度计算与候选管理。

**主要特性**:
- 三平台一键查询
- 商业 API 统一网关
- 热度计算与排名
- 候选管理与筛选
- CSV/XLSX 导入导出

**技术实现**:
- `src/services/commercial_search.py` - 商业搜索服务
- `src/services/candidate.py` - 候选管理服务
- `src/services/heat.py` - 热度计算服务
- `src/adapters/` - 平台适配器

### 2.2 视频音轨转文案

**功能描述**: 支持 MP4/MOV 视频上传，通过 FFmpeg 提取音轨，使用 faster-whisper 进行本地语音识别。

**主要特性**:
- 视频上传与验证
- 音轨提取与预处理
- 本地语音识别
- 在线校对与编辑
- 多格式导出（TXT/JSON/SRT）

**技术实现**:
- `src/services/transcription.py` - 转写服务
- `src/resources.py` - FFmpeg 资源管理
- `src/asr_quality.py` - ASR 质量评估
- `app_pages/transcription.py` - 转写页面

### 2.3 数字人生成

**功能描述**: 文案驱动视频生成，支持多形象、多音色，异步任务管理。

**主要特性**:
- 文案输入与验证
- 形象与音色选择
- 异步任务提交
- 结果验证与存储
- 状态追踪与管理

**技术实现**:
- `src/services/avatar.py` - 数字人服务
- `src/adapters/avatar.py` - 数字人适配器
- `app_pages/avatar_generation.py` - 数字人页面

### 2.4 批量生产流水线

**功能描述**: 端到端自动化编排，支持任务队列管理与进度追踪。

**主要特性**:
- 流水线配置
- 任务队列管理
- 进度追踪
- 错误处理与重试
- 结果聚合

**技术实现**:
- `src/services/pipeline.py` - 流水线服务
- `app_pages/pipeline.py` - 流水线页面

### 2.5 多平台发布

**功能描述**: 支持抖音、快手、视频号等平台的一键发布。

**主要特性**:
- 多平台适配
- 沙箱与生产模式
- 状态追踪
- 失败重试与回滚
- 发布记录管理

**技术实现**:
- `src/services/publisher.py` - 发布服务
- `src/adapters/` - 平台发布适配器

### 2.6 系统管理

**功能描述**: 系统状态监控、任务管理、配置管理。

**主要特性**:
- 系统状态监控
- 任务记录查看
- 阻断任务处理
- 配置管理
- 日志查看

**技术实现**:
- `app_pages/admin.py` - 管理页面
- `app_pages/tasks.py` - 任务页面

---

## 3. 本地启动步骤

### 3.1 环境准备

#### 3.1.1 必需软件

| 软件 | 版本要求 | 用途 | 下载地址 |
|------|----------|------|----------|
| Python | 3.12+ | 后端运行环境 | https://www.python.org/downloads/ |
| Node.js | 18+ | 前端构建环境 | https://nodejs.org/ |
| FFmpeg | 最新版 | 视频处理 | https://ffmpeg.org/download.html |
| Git | 最新版 | 版本控制 | https://git-scm.com/downloads |

#### 3.1.2 环境检查

```powershell
# 检查 Python
python --version

# 检查 Node.js
node --version

# 检查 FFmpeg
ffmpeg -version

# 检查 Git
git --version
```

### 3.2 项目获取

```powershell
# 克隆项目
git clone <repository-url>
cd video

# 或者解压项目压缩包
```

### 3.3 一键启动（推荐）

#### Windows 用户

```powershell
# 方式1：双击 start.bat 文件
# 方式2：命令行执行
start.bat
```

#### PowerShell 用户

```powershell
# 执行启动脚本
.\scripts\start_all_services.ps1

# 可选参数
.\scripts\start_all_services.ps1 -SkipBrowser      # 跳过浏览器自动打开
.\scripts\start_all_services.ps1 -SkipHealthCheck   # 跳过健康检查（快速启动）
```

### 3.4 手动启动

#### 步骤 1：启动 FastAPI 后端

```powershell
# 设置 PYTHONPATH（后端需要访问 src/ 目录）
$env:PYTHONPATH = "C:\Users\zeng\Desktop\video"

# 进入后端目录
cd project/backend

# 安装后端依赖
python -m pip install -r requirements.txt

# 启动 FastAPI 服务
python -m uvicorn app.main:app --host 0.0.0.0 --port 2001
```

**访问地址**: http://localhost:2001  
**API 文档**: http://localhost:2001/docs

#### 步骤 2：启动 React 前端

```powershell
# 进入前端目录
cd project/frontend

# 安装前端依赖
npm install

# 启动开发服务器
npm run dev
```

**访问地址**: http://localhost:1001

### 3.5 服务验证

启动完成后，验证以下服务是否正常：

1. **FastAPI 后端**: http://localhost:2001/health - 应返回健康状态
2. **React 前端**: http://localhost:1001 - 应显示前端界面
3. **API 文档**: http://localhost:2001/docs - 应显示 Swagger UI

---

## 4. 线上部署说明

### 4.1 部署架构

```
                    ┌─────────────────┐
                    │   负载均衡器    │
                    │   (Nginx/CDN)   │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐
│  React 前端   │    │ FastAPI 后端  │
│  (静态文件)   │    │  (API 服务)   │
└───────────────┘    └───────────────┘
        │                    │
        └────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   数据库服务    │
                    │  (PostgreSQL)   │
                    └─────────────────┘
```

### 4.2 环境配置

#### 4.2.1 环境变量

创建 `.env` 文件：

```env
# 应用配置
APP_ENV=production
APP_DEBUG=false
APP_SECRET_KEY=your-secret-key-here

# 数据库配置
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=video
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password

# 服务端口
FASTAPI_PORT=2001
FRONTEND_PORT=1001

# 视频处理
FFMPEG_PATH=/usr/bin/ffmpeg
MAX_VIDEO_SIZE_MB=50

# 语音识别
WHISPER_MODEL=large-v3-turbo
WHISPER_DEVICE=cpu

# 数字人服务（可选）
AVATAR_SERVICE_ENABLED=false
AVATAR_SERVICE_BASE_URL=http://avatar-service:8080
AVATAR_SERVICE_TOKEN=your-avatar-token

# 平台 API（可选）
VIDEO_LICENSED_PROVIDER_MODE=sandbox
DOUYIN_CLIENT_KEY=
DOUYIN_CLIENT_SECRET=
```

#### 4.2.2 数据库初始化

```sql
-- PostgreSQL 初始化
CREATE DATABASE video;
CREATE USER video_user WITH PASSWORD 'your-password';
GRANT ALL PRIVILEGES ON DATABASE video TO video_user;

-- 执行初始化脚本
\c video
\i database/scripts/init.postgres.sql
```

### 4.3 部署步骤

#### 4.3.1 服务器准备

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装依赖
sudo apt install -y python3.12 python3.12-venv nodejs npm ffmpeg postgresql nginx

# 创建项目目录
sudo mkdir -p /opt/video-intelligence
sudo chown $USER:$USER /opt/video-intelligence
```

#### 4.3.2 项目部署

```bash
# 克隆项目
cd /opt/video-intelligence
git clone <repository-url> .

# 创建 Python 虚拟环境
python3.12 -m venv venv
source venv/bin/activate

# 安装 Python 依赖
pip install -r requirements.txt
pip install -r project/backend/requirements.txt

# 构建前端
cd project/frontend
npm install
npm run build
cd ../..

# 初始化数据库
psql -U postgres -d video -f database/scripts/init.postgres.sql
```

#### 4.3.3 服务配置

**Systemd 服务文件** (`/etc/systemd/system/video-fastapi.service`):

```ini
[Unit]
Description=Video Intelligence FastAPI Service
After=network.target postgresql.service

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/video-intelligence/project/backend
Environment="PATH=/opt/video-intelligence/venv/bin"
ExecStart=/opt/video-intelligence/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 2001
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### 4.3.4 Nginx 配置

```nginx
# /etc/nginx/sites-available/video-intelligence
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /opt/video-intelligence/project/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # FastAPI 后端
    location /api/ {
        proxy_pass http://localhost:2001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }


    # 健康检查
    location /health {
        proxy_pass http://localhost:2001/health;
    }
}
```

#### 4.3.5 启动服务

```bash
# 启用并启动服务
sudo systemctl enable video-fastapi
sudo systemctl start video-fastapi

# 启用 Nginx 配置
sudo ln -s /etc/nginx/sites-available/video-intelligence /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 检查服务状态
sudo systemctl status video-fastapi
sudo systemctl status nginx
```

### 4.4 Docker 部署（可选）

#### 4.4.1 Dockerfile

```dockerfile
# 多阶段构建
FROM node:18-alpine AS frontend-builder
WORKDIR /app
COPY project/frontend/package*.json ./
RUN npm ci
COPY project/frontend/ .
RUN npm run build

FROM python:3.12-slim
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    ffmpeg \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY project/backend/requirements.txt ./backend-requirements.txt
RUN pip install --no-cache-dir -r backend-requirements.txt

# 复制应用代码
COPY . .
COPY --from=frontend-builder /app/dist project/frontend/dist

# 暴露端口
EXPOSE 1001 2001

# 启动脚本
COPY scripts/docker-entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
```

#### 4.4.2 docker-compose.yml

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: video
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: your-password
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/scripts/init.postgres.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

    app:
    build: .
    ports:
      - "1001:1001"
      - "2001:2001"
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=video
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=your-password
    depends_on:
      - postgres
    volumes:
      - app_data:/app/data

volumes:
  postgres_data:
  app_data:
```

#### 4.4.3 启动命令

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 4.5 监控与维护

#### 4.5.1 日志管理

```bash
# FastAPI 日志
journalctl -u video-fastapi -f

# Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

#### 4.5.2 健康检查

```bash
# 检查服务状态
curl http://localhost:2001/health

# 检查端口监听
netstat -tlnp | grep -E '1001|2001'

# 检查进程
ps aux | grep -E 'vite|uvicorn'
```

#### 4.5.3 备份策略

```bash
# 数据库备份
pg_dump -U postgres video > backup_$(date +%Y%m%d_%H%M%S).sql

# 文件备份
tar -czf data_backup_$(date +%Y%m%d_%H%M%S).tar.gz /opt/video-intelligence/data/

# 自动备份脚本
cat > /opt/video-intelligence/scripts/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/video-intelligence/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
pg_dump -U postgres video > $BACKUP_DIR/db_$DATE.sql

# 备份数据文件
tar -czf $BACKUP_DIR/data_$DATE.tar.gz /opt/video-intelligence/data/

# 清理30天前的备份
find $BACKUP_DIR -name "*.sql" -mtime +30 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete
EOF

chmod +x /opt/video-intelligence/scripts/backup.sh

# 添加定时任务
echo "0 2 * * * /opt/video-intelligence/scripts/backup.sh" | crontab -
```

---

## 5. 常见问题处理

### 5.1 启动问题

#### Q1: Python 依赖安装失败

**症状**: `pip install` 报错，网络连接超时

**解决方案**:
```powershell
# 使用国内镜像源
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者使用阿里云镜像
python -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

#### Q2: 端口被占用

**症状**: 启动时提示端口已被占用

**解决方案**:
```powershell
# 查看端口占用
netstat -ano | findstr "2001 1001"

# 停止占用进程
taskkill /PID <进程ID> /F

# 或者使用启动脚本自动处理
.\scripts\start_all_services.ps1
```

#### Q3: FFmpeg 未安装

**症状**: 视频处理功能无法使用

**解决方案**:
```powershell
# Windows (使用 winget)
winget install ffmpeg

# 或使用 chocolatey
choco install ffmpeg

# 或手动下载
# 访问 https://ffmpeg.org/download.html 下载
# 解压后添加到 PATH 环境变量
```

#### Q4: Node.js 版本过低

**症状**: 前端构建失败，提示语法错误

**解决方案**:
```powershell
# 检查版本
node --version

# 升级 Node.js
# 方式1：使用 nvm-windows
nvm install 18
nvm use 18

# 方式2：官网下载
# 访问 https://nodejs.org/ 下载最新 LTS 版本
```

### 5.2 运行问题

#### Q5: 数据库连接失败

**症状**: 应用启动后无法访问数据

**解决方案**:
```powershell
# 检查 SQLite 文件权限
icacls data\video_intelligence.db

# 检查 PostgreSQL 连接
psql -U postgres -d video -c "SELECT 1;"

# 检查环境变量
echo $env:POSTGRES_HOST
```

#### Q6: 语音识别失败

**症状**: 转写功能无法使用

**解决方案**:
```powershell
# 检查 faster-whisper 安装
python -c "import faster_whisper; print('OK')"

# 检查模型下载
# 首次使用会自动下载模型，请确保网络通畅

# 检查 FFmpeg
ffmpeg -version
ffprobe -version
```

#### Q7: 前端无法访问后端 API

**症状**: 前端页面显示，但数据加载失败

**解决方案**:
```powershell
# 检查后端服务是否启动
curl http://localhost:2001/health

# 检查 CORS 配置
# 查看 project/backend/app/main.py 中的 CORS 设置

# 检查代理配置
# 查看 project/frontend/vite.config.ts 中的 proxy 配置
```

### 5.3 性能问题

#### Q8: 应用响应缓慢

**症状**: 页面加载慢，操作卡顿

**解决方案**:
```powershell
# 检查系统资源
tasklist | findstr "python node"

# 检查数据库大小
dir data\video_intelligence.db

# 清理临时文件
Remove-Item -Path data\backups\* -Force -ErrorAction SilentlyContinue

# 重启服务
.\scripts\start_all_services.ps1
```

#### Q9: 内存占用过高

**症状**: 系统内存不足警告

**解决方案**:
```powershell
# 检查内存使用
Get-Process | Sort-Object WorkingSet64 -Descending | Select-Object -First 10

# 调整 Python 内存
# 设置环境变量 PYTHONMALLOC=malloc
```

### 5.4 数据问题

#### Q10: 数据库损坏

**症状**: SQLite 数据库无法访问

**解决方案**:
```powershell
# 备份当前数据库
Copy-Item data\video_intelligence.db data\video_intelligence.db.backup

# 尝试修复
sqlite3 data\video_intelligence.db "PRAGMA integrity_check;"

# 如果损坏严重，从备份恢复
Copy-Item data\backups\video_intelligence.db data\video_intelligence.db
```

#### Q11: 媒体文件丢失

**症状**: 视频或音频文件无法访问

**解决方案**:
```powershell
# 检查文件权限
icacls data\video_edits\*

# 检查磁盘空间
Get-PSDrive C

# 重新生成缩略图
python scripts/regenerate_thumbnails.py
```

---

## 6. 关键代码目录索引

### 6.1 项目根目录

```
video/
├── app.py                    # legacy Streamlit 源码，仅供追溯
├── start.bat                 # Windows 一键启动脚本
├── requirements.txt          # Python 依赖清单
├── README.md                 # 项目说明文档
├── PROJECT_HANDOFF.md        # 项目交接文档（本文件）
└── .gitignore                # Git 忽略配置
```

### 6.2 核心源代码 (`src/`)

```
src/
├── __init__.py               # 包初始化
├── models.py                 # 数据模型定义
│   ├── VideoCandidate        # 视频候选模型
│   ├── VideoMetricSnapshot   # 视频指标快照
│   ├── HeatResult            # 热度结果
│   ├── TaskRecord            # 任务记录
│   └── TranscriptionTask     # 转写任务
├── contracts.py              # 接口协议定义
│   ├── CrawlerAdapter        # 爬虫适配器接口
│   ├── LicensedSearchProvider # 授权搜索提供者
│   ├── AvatarProvider        # 数字人提供者
│   └── Repository            # 仓储接口
├── services/                 # 业务服务层（14 个模块）
│   ├── __init__.py
│   ├── candidate.py          # 候选管理服务
│   ├── commercial_search.py  # 商业搜索服务
│   ├── heat.py               # 热度计算服务
│   ├── transcription.py      # 转写服务
│   ├── avatar.py             # 数字人服务
│   ├── pipeline.py           # 流水线服务
│   ├── publisher.py          # 发布服务
│   ├── copywriting.py        # 文案服务
│   ├── video_editor.py       # 视频编辑服务
│   ├── video_source.py       # 视频源管理
│   ├── discovery.py          # 关键词发现
│   ├── keyword_trend.py      # 关键词趋势
│   └── source.py             # 数据源管理
├── repositories/             # 数据访问层
│   ├── __init__.py
│   ├── sqlite.py             # SQLite 仓储实现
│   └── mock.py               # Mock 仓储（测试用）
├── adapters/                 # 外部服务适配器
│   ├── __init__.py
│   ├── asr_bridge.py         # ASR 桥接层
│   ├── aliyun_asr.py         # 阿里云 ASR 适配器
│   ├── avatar.py             # 数字人适配器
│   ├── douyin.py             # 抖音适配器
│   ├── xiaohongshu.py        # 小红书适配器
│   └── wechat_channels.py    # 视频号适配器
├── resources.py              # 资源管理（FFmpeg、ASR）
├── asr_quality.py            # ASR 质量评估
├── app_state.py              # 应用状态管理
└── retry_policy.py           # 重试策略
```

### 6.3 legacy Streamlit 页面 (`app_pages/`)

```
app_pages/
├── __init__.py
├── candidates.py             # 爆火视频检索页面
├── transcription.py          # 视频音轨转文案页面
├── avatar_generation.py      # 数字人生成页面
├── pipeline.py               # 批量生产流水线页面
├── tasks.py                  # 任务记录页面
└── admin.py                  # 系统管理页面
```

### 6.4 React 前端 (`project/frontend/`)

```
project/frontend/
├── package.json              # 前端依赖配置
├── vite.config.ts            # Vite 构建配置
├── tsconfig.json             # TypeScript 配置
├── src/                      # 源代码目录
│   ├── main.tsx              # 应用入口
│   ├── App.tsx               # 根组件（Router + ErrorBoundary + 全局快捷键）
│   ├── pages/                # 页面组件（11 个）
│   │   ├── DashboardPage.tsx     # 数据仪表盘（业务数据可视化）
│   │   ├── CandidatesPage.tsx    # 候选检索页面
│   │   ├── PipelinePage.tsx      # 批量生产流水线
│   │   ├── TranscriptionPage.tsx # 语音转写（支持链接/文件）
│   │   ├── TasksPage.tsx         # 任务中心
│   │   ├── AdminPage.tsx         # 系统管理
│   │   ├── AnalyticsPage.tsx     # 深度分析（Pro）
│   │   ├── AiCopyPage.tsx        # AI文案生成（Pro）
│   │   ├── AvatarPage.tsx        # 数字人视频生成
│   │   ├── PublishPage.tsx       # 多平台发布
│   │   ├── KeywordCrawlerPage.tsx # 关键词爬虫
│   │   └── NotFoundPage.tsx      # 404 页面
│   ├── components/           # 通用组件（8 个）
│   │   ├── Sidebar.tsx           # 侧边栏（折叠/展开、导航分组）
│   │   ├── TopHeader.tsx         # 顶部栏（通知/消息/个人中心）
│   │   ├── ErrorBoundary.tsx     # 全局错误边界
│   │   ├── GlobalSearchModal.tsx # 全局搜索（Ctrl+K）
│   │   ├── ShortcutHelpModal.tsx # 快捷键帮助（Ctrl+/）
│   │   ├── Toast.tsx             # 全局 Toast 提示
│   │   └── SkeletonLoader.tsx    # 骨架屏加载
│   ├── layouts/              # 布局组件
│   │   └── DashboardLayout.tsx   # 主布局（侧边栏+顶栏+内容区）
│   ├── contexts/             # React Context
│   │   └── ThemeContext.tsx       # 主题上下文（深色/浅色模式）
│   ├── api/                  # API 客户端与类型定义
│   │   ├── client.ts         # API 客户端（中文错误提示）
│   │   └── types.ts          # TypeScript 类型定义
│   ├── hooks/                # 自定义 Hook
│   │   └── useKeyboardShortcuts.ts # 键盘快捷键管理
│   ├── styles/               # 样式文件
│   │   ├── variables.css     # CSS 变量（主题色、间距等）
│   │   └── theme.ts          # Ant Design 主题配置
│   └── utils/                # 工具函数
├── public/                   # 静态资源
└── dist/                     # 构建输出
```

**前端路由结构**:

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | - | 重定向到 `/candidates` |
| `/dashboard` | DashboardPage | 数据仪表盘 |
| `/candidates` | CandidatesPage | 候选检索 |
| `/pipeline` | PipelinePage | 批量生产 |
| `/transcription` | TranscriptionPage | 语音转写 |
| `/tasks` | TasksPage | 任务中心 |
| `/admin` | AdminPage | 系统管理 |
| `/analytics` | AnalyticsPage | 深度分析（Pro） |
| `/ai-copy` | AiCopyPage | AI文案生成（Pro） |
| `/avatar` | AvatarPage | 数字人生成 |
| `/publish` | PublishPage | 多平台发布 |
| `/crawler` | KeywordCrawlerPage | 关键词爬虫 |

### 6.5 FastAPI 后端 (`project/backend/`)

```
project/backend/
├── requirements.txt          # 后端依赖
├── app/                      # 应用代码
│   ├── main.py               # FastAPI 应用入口
│   ├── api/                  # API 路由
│   │   └── v1/               # API v1 版本
│   │       ├── candidates.py # 候选 API
│   │       ├── transcriptions.py # 转写 API
│   │       ├── pipelines.py  # 流水线 API
│   │       ├── tasks.py      # 任务 API
│   │       ├── admin.py      # 管理 API
│   │       ├── copywriting.py # 文案 API
│   │       ├── video_editor.py # 视频编辑 API
│   │       └── publish.py    # 发布 API
│   ├── core/                 # 核心配置
│   │   ├── config.py         # 配置管理
│   │   └── security.py       # 安全配置
│   ├── models/               # 数据模型
│   ├── schemas/              # Pydantic 模式
│   └── services/             # 业务服务
└── tests/                    # 测试代码
```

### 6.6 数据库 (`database/`)

```
database/
├── README.md                 # 数据库说明
├── scripts/                  # SQL 脚本
│   └── init.postgres.sql     # PostgreSQL 初始化脚本
└── migrations/               # 数据库迁移框架
    ├── runner.py             # MigrationRunner 核心类
    ├── 001_initial_schema.py # 初始 Schema（15 张表 + 6 索引）
    ├── 002_add_column_migrations.py # 列扩展迁移
    ├── __main__.py           # CLI 入口
    └── __init__.py           # 包初始化
```

### 6.7 脚本工具 (`scripts/`)

```
scripts/
├── start_all_services.ps1    # 全服务启动脚本
├── launch_app.ps1            # 单应用启动脚本
├── benchmark_asr.py          # ASR 基准测试
└── backup.sh                 # 备份脚本（示例）
```

### 6.8 测试代码 (`tests/`)

```
tests/
├── __init__.py
├── test_models.py                # 模型测试
├── test_streamlit_pages.py       # legacy Streamlit 页面测试
├── test_repository_and_services.py # 仓储与服务集成测试
├── test_commercial_search.py     # 商业搜索测试
├── test_ingestion_and_sqlite.py  # 数据入库测试
├── test_asr_quality.py           # ASR 质量测试
├── test_avatar_integration.py    # 数字人集成测试
├── test_oneapi_adapter.py        # OneAPI 适配器测试
├── test_pipeline.py              # 流水线测试
├── test_video_source.py          # 视频源测试
├── test_keyword_discovery.py     # 关键词发现测试
├── test_real_transcription.py    # 真实转写测试
├── test_llm_adapter.py           # LLM 适配器测试
├── test_publisher_service.py     # 发布服务测试
├── test_video_editor_service.py  # 视频编辑服务测试
├── test_copywriting_service.py   # 文案服务测试
├── test_asr_bridge.py            # ASR 桥接测试
└── test_migration_runner.py      # 迁移框架测试
```

### 6.9 文档 (`docs/`)

```
docs/
├── archive/                  # 历史文档
│   └── video-intelligence-production-dev-doc-full-v1.6.md # 完整设计文档
├── api/                      # API 文档
└── guides/                   # 使用指南
```

### 6.10 数据文件 (`data/`)

```
data/
├── video_intelligence.db     # SQLite 数据库
├── video_edits/              # 视频编辑文件
├── avatar_results/           # 数字人生成结果
└── backups/                  # 备份文件
```

---

## 7. 开发规范

### 7.1 代码规范

- **Python**: 遵循 PEP 8，使用 Ruff 进行代码检查
- **TypeScript**: 遵循 ESLint 规则
- **提交信息**: 使用约定式提交格式

### 7.2 分支管理

- `main`: 主分支，保持稳定
- `develop`: 开发分支
- `feature/*`: 功能分支
- `hotfix/*`: 紧急修复分支

### 7.3 测试要求

- 单元测试覆盖率 > 80%
- 集成测试覆盖核心流程
- E2E 测试覆盖关键用户路径

### 7.4 文档要求

- 代码注释清晰
- API 文档完整
- 变更日志及时更新

---

## 8. 联系方式

- **技术负责人**: Crow5 开发团队
- **邮箱**: dev@example.com
- **文档**: https://docs.example.com/video-intelligence

---

## 9. 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 2.2.0 | 2026-07-20 | React 前端大幅增强：11 个页面、8 个组件、全局搜索/快捷键、通知/消息/个人中心抽屉、数字人页面、深度分析/AI文案页面 |
| 2.1.0 | 2026-07-20 | 三服务架构完成：FastAPI 后端（9 路由）、React 前端（6 页面）、数据库迁移框架、ASR 桥接层、~290 个测试 |
| 2.0.0 | 2026-07-19 | 全新架构，支持三平台、数字人、流水线 |
| 1.6.0 | 2026-07-18 | 完善转写功能，优化准确率 |
| 1.5.0 | 2026-07-17 | 新增关键词发现、趋势分析 |
| 1.0.0 | 2026-07-16 | 初始版本，基础功能实现 |

---

## 10. 最近更新（v2.2.0）

### 10.1 新增页面

| 页面 | 路由 | 功能 |
|------|------|------|
| DashboardPage | `/dashboard` | 数据仪表盘，展示业务数据可视化（生产趋势、任务分布、热门候选） |
| AnalyticsPage | `/analytics` | 深度分析，Pro 功能（趋势表格、竞品分析、内容分布） |
| AiCopyPage | `/ai-copy` | AI 文案生成，Pro 功能（主题输入、风格选择、文案结果） |
| AvatarPage | `/avatar` | 数字人视频生成（形象录制/上传、TTS/录制/上传音频、视频生成） |
| PublishPage | `/publish` | 多平台发布 |
| KeywordCrawlerPage | `/crawler` | 关键词爬虫 |

### 10.2 新增组件

| 组件 | 功能 |
|------|------|
| Sidebar.tsx | 侧边栏，支持折叠/展开、导航分组、Pro 标识 |
| TopHeader.tsx | 顶部栏，包含通知抽屉、消息抽屉、个人中心抽屉、账号设置跳转 |
| GlobalSearchModal.tsx | 全局搜索弹窗（Ctrl+K 触发） |
| ShortcutHelpModal.tsx | 快捷键帮助弹窗（Ctrl+/ 触发） |
| Toast.tsx | 全局 Toast 提示组件 |
| SkeletonLoader.tsx | 骨架屏加载组件 |

### 10.3 新增功能

1. **通知中心**: 5 条 mock 通知，支持标记已读、全部已读、删除
2. **消息中心**: 3 条 mock 消息，支持标记已读
3. **个人中心**: 用户信息展示（用户名、邮箱、手机、角色、密码、两步验证）
4. **账号设置**: 跳转到 `/admin` 系统管理页
5. **全局搜索**: Ctrl+K 打开，支持页面导航
6. **快捷键帮助**: Ctrl+/ 打开，展示所有快捷键
7. **语音转写增强**: 支持链接转写和文件上传转写
8. **数字人生成**: 支持摄像头录制、照片上传、TTS、音频录制

### 10.4 技术改进

- 侧边栏折叠时自动隐藏文字
- Pro 会员直接进入 Pro 功能页面（不再弹升级窗）
- 顶栏显示通知/消息未读数角标
- 深色/浅色主题切换
- 键盘快捷键支持

---

**文档维护**: 请在每次重大更新后同步更新本文档
**最后审核**: 2026-07-20
**下次计划审核**: 2026-08-20
