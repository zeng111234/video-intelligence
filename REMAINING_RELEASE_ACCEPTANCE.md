# VideoInsight 最终版本剩余功能与按钮验收矩阵

2026-08-10 最新本地整体验收、真实 Chrome 证据和暂缓边界见 `本地整体验收报告-2026-08-10.md`；此前收尾背景见 `上线收尾报告-2026-08-10.md`。

这份清单只记录尚未获得真实交付证据的项目。自动化测试、类型检查或构建通过，不等于按钮已经在客户电脑和公司服务器之间真实跑通。每完成一组，都要保存日期、身份、设备、结果和必要截图；失败时先保留数据并定位，不自动重复付费。

## 当前已完成的底层核验（2026-08-10）

- Python 共收集 1,253 项发布测试并全部通过，包含 FastAPI、公司控制层、服务适配器、性能基线和旧页面测试。
- React 共 155 项测试通过，TypeScript 类型检查和 Vite 正式构建通过；桌面密钥隔离、更新与安装包哈希保护 7 项测试通过。
- 正式版本必须显式填写且高于历史 `0.2.0`；同版本安装包与服务器更新文件均拒绝覆盖，更新清单拒绝回退，客户端拒绝“清单版本与安装包文件名不一致”。
- 覆盖安装会把旧程序保留到新版本自动验收通过之后；失败路径具备程序目录和卸载信息恢复逻辑。脚本语法、C# 编译和回归测试已通过，真实断网回滚仍待最终内部电脑验收。
- 服务器部署包只带入现有“大树1”形象和声音的供应商编号；不含训练样本、供应商密钥、数据库、日志或媒体文件，部署时不会提交训练。
- 已用隔离的本机控制层与桌面客户端完成真实页面联调：服务器测试激活码登录、100 积分、本机工作台、管理员只读数据和收费项目折叠均通过，浏览器控制台无业务错误。
- 普通客户页面已真实显示并自动选中“大树1”形象和声音，明确为训练完成、无需重新训练；只查看了预计积分，没有提交生成。
- 桌面运行目录已验证只绑定首次客户：第二个测试激活码返回 409，且服务器没有保留其有效会话。该证据仍不能替代正式 HTTPS 和跨电脑验收。
- 当前生成的 `avatarcheck` ZIP 仅供本机结构核验，不是客户安装包，也不要发送给客户。
- 普通客户 14 个子页面、智能创作筛选框、390px 移动端菜单、沙箱转写、免费剪辑体验和发布成片上传已在真实 Chrome 中通过；验收 API 指向隔离的 2101，不修改正常 1001/2001 客户数据。
- 抖音官网真实免费搜索已请求并返回 30 条，确认多列和 180 天筛选，无错误、无积分消耗；专用测试 Chrome 已关闭。
- 全部交付用 PowerShell 脚本已转换为 UTF-8 with BOM，并新增 Windows PowerShell 5 逐文件解析门禁，避免客户机再次出现乱码语法错误。
- 发布门禁现已覆盖公司控制层、根服务、旧页面、React、桌面更新、类型和正式构建；开发模式完整门禁通过。
- 最终 EXE 脚本会拒绝本机/示例域名、已有同版本、脏工作区、未通过在线检查的公司服务，以及包含运行数据、媒体、私钥或构建机供应商密钥的包。现有示例域名 0.2.0 不可交付。
- 生产控制层会拒绝示例域名、内存会话、开放接口文档和不完整供应商配置；云转写与云剪辑使用独立开关，不会因开启其中一项而意外开放另一项收费能力。
- 重启前未完成或超时的收费操作会进入“结果未知、禁止自动重提”，不会在恢复后重复扣费；备份恢复会撤销旧会话并精确还原数据，不会把旧文件叠加回来。
- 连接公司控制层时，转写、发布和智能创作后台任务在没有当前绑定客户会话时不会被领取；普通业务与管理员接口也已按路径只发送一个正确角色，避免管理员身份绕过客户积分。
- 管理员退出现在会撤销公司控制层会话；生产前端依赖审计为 0。开发工具只剩 Vite 5 本地开发服务器公告，Vite 与 `node_modules` 不进入客户运行包。

## 1. 公司服务器与账号（不产生供应商费用）

| 身份/页面 | 必须逐项操作 | 通过标准 | 当前状态 |
| --- | --- | --- | --- |
| 普通客户登录 | 激活码登录、错误码、禁用码、退出、会话过期后重登 | 只进入自己的数据与积分；错误提示可恢复 | 正确登录、余额、第二客户隔离及双角色身份自动化已通过；退出实页和其余项待正式 HTTPS |
| 管理员登录 | 正确/错误密码、退出、新增管理员、重置密码 | 普通客户无法进入管理接口；旧会话按规则失效 | 正确登录与权限隔离已本机通过，退出撤销已自动化通过；新实页点击和其余项待正式 HTTPS |
| 激活码管理 | 生成、复制、启用/禁用、充值、刷新、分页/大量数据 | 操作有审计记录；禁用后客户会话失效 | 服务器列表与余额已真实加载；写操作和大量数据待跨电脑 |
| 充值审批 | 客户申请、管理员批准/拒绝、重复点击、列表刷新 | 只到账一次；拒绝不加积分；空/多条列表清楚 | 待跨电脑 |
| 定价与余额 | 查看价格、修改价格、余额与流水刷新 | 服务器为唯一权威；客户端改文件不能改余额 | 查看、折叠和服务器余额已本机通过；修改待正式服务器 |
| 服务状态 | 刷新文案/转写/云成片/数字人状态 | 不返回密钥；爬虫显示本地且不扣积分 | 本机双进程通过；正式供应商状态待配置 |

## 2. 本地免费能力（不扣积分）

| 页面 | 必须逐项操作 | 通过标准 | 当前状态 |
| --- | --- | --- | --- |
| 智能创作/专业爬虫 | 关键词、平台、单/多列、筛选、分页/加载更多、取消、重试 | 筛选真实生效；数量与平台返回一致；爬虫不扣积分 | 抖音真实官网搜索 30/30、多列、180 天筛选和 0 积分已通过；其他平台与最终 EXE 仍待复验 |
| 候选素材 | 预览、原链接、选择/取消、批量选择、带入下一步 | 选择不会丢失；错误链接不阻断其他素材 | 待最终 EXE |
| 上传与本地预览 | 选择文件、取消、格式/大小错误、播放 | 不上传到公司服务器，除非用户明确进入收费能力 | 转写、剪辑和发布上传实页通过；格式/大小边界自动化通过，待最终 EXE 复验 |
| 任务列表 | 搜索、筛选、展开、查看原因、继续、删除/取消确认 | 刷新后状态可信；只显示当前客户任务 | 待跨电脑 |
| 帮助与导航 | 全部菜单、返回、折叠、移动端菜单、个人中心 | 无死链、无空白页、无无效占位信息 | 普通客户 14 个路由和移动菜单实页通过；待最终 EXE 与个人中心复验 |

## 3. 现有数字人资产复用（导入本身不收费）

| 项目 | 必须逐项操作 | 通过标准 | 当前状态 |
| --- | --- | --- | --- |
| “大树1”形象 | 服务器首次部署导入、普通客户查看和选择 | 显示 `ready`；不出现重新训练；EXE 不含供应商密钥 | 本机双进程页面已通过；待正式线上复验 |
| “大树1”声音 | 查看和选择现有克隆声音 | 使用已有供应商编号；不上传训练样本；不发起克隆训练 | 本机双进程页面已通过；待正式线上复验 |
| 权限隔离 | 共享资产与客户私有资产分别查看 | 公司共享资产所有客户可见；私有资产只对创建者可见 | 自动化及单机客户绑定通过；待线上多客户复验 |

## 4. 收费能力（必须逐项单独批准，每项只测一条）

| 能力 | 付费前必须显示 | 最低成本真实验收 | 当前状态 |
| --- | --- | --- | --- |
| AI 文案 | 预计积分、供应商费用范围、输入摘要 | 短文案一次；成功只扣一次，失败按规则退回 | 客户/管理员身份路由已修复；真实扣分仍待服务器密钥与批准 |
| 云端转写 | 音视频时长、预计积分、单条上限、失败是否扣费 | 短音频一次；结果、低置信度 AI 复核、重复提交保护 | 待服务器密钥与批准 |
| 云成片 | 素材数量、清晰度、预计总积分与供应商费用 | 最短合规素材一次；提交、轮询、下载、失败恢复 | 待服务器密钥与批准 |
| 数字人成片 | “大树1”形象/声音、文案、预计积分、不会重新训练 | 极短文案一次；验证真实视频 URL、下载与只扣一次 | 待服务器密钥与批准 |
| 真实发布 | 平台账号、目标内容、不可逆说明 | 只在专用测试账号明确批准后发布一条 | 待账号与批准 |

## 5. 异常、恢复与更新（不允许用成功提示代替真实结果）

- [x] 断网、超时和供应商结果未知：自动化与沙箱恢复已证明最多自动重试一次、保留任务和素材、不自动重复扣费；真实收费供应商仍需获批后复验。
- [x] 重复点击、刷新和后端重启：自动化已证明同一幂等操作只执行和计费一次；最终 EXE 关闭重开仍在干净电脑复验。
- [ ] 服务器重启、数据库备份与恢复：自动化已覆盖重启未完成操作、旧会话撤销、精确恢复和恶意归档拦截；仍需在正式服务器执行一次真实备份/恢复演练，确认积分和任务记录不丢失。
- [ ] 更新提示、取消下载、哈希不匹配、下载中断、安装升级：必须由用户确认，不静默安装。
- [ ] 覆盖安装自动验收失败：旧程序、旧卸载版本和客户数据恢复，旧版可重新启动。
- [ ] 空数据、1 条、10 条以上和大量数据：列表、分页、折叠和移动端布局均可用。
- [ ] 最终安装包在一台没有 Python、Node、Git、FFmpeg 的 Windows 电脑上只双击即可运行。
- [ ] 同一浏览器先后登录普通客户和管理员后，普通业务仍按客户积分执行，管理接口只认管理员；分别退出时只有对应会话失效。

## 6. 最终放行条件

- [ ] 本矩阵所有“不收费”项目完成真实浏览器/EXE 验收。
- [ ] 每项收费能力获得负责人批准并只完成一次最低成本真实验收。
- [ ] `PRODUCTION_RELEASE_CHECKLIST.md` 全部勾选并保留证据。
- [ ] 使用未出现过的新版本号，仅生成并发送一个正式安装包，核对 SHA256；不得覆盖或发送现有示例域名 0.2.0。
