# 功能与按钮测试问题清单

测试日期：2026-08-08
测试目标：交付前全量巡检；先记录问题，再完成已确认问题的修复与回归。

## 结论

- P1-001 和 P2-001 已修复：两个任务记录入口都能把 `OUTCOME_UNKNOWN` 显示为“结果待核对”，不再崩溃或暴露内部枚举值。
- 已为 Streamlit 和 React 任务页补充对应回归测试；后端全量测试、前端 126 项测试、类型检查和生产构建均通过。
- 仍不建议直接交付：上线范围尚未冻结，工作区存在大量未提交改动；付费、真实发布、充值审批、删除/禁用等高副作用链路也尚未做专项验收。

## 交付阻断问题

### P1-001（已修复）：任务记录页遇到 `OUTCOME_UNKNOWN` 状态会崩溃

- 页面：Streamlit「任务记录」
- 位置：[app_pages/tasks.py](app_pages/tasks.py) 第 74 行
- 前置条件：任务数据中存在 `TaskStatus.OUTCOME_UNKNOWN`。当前仓库数据库/测试数据已经存在该状态。
- 复现命令：

  ```powershell
  $env:API_KEY='pytest-api-key'
  $env:ADMIN_PASSWORD='pytest-admin-password'
  python -m pytest -q tests/test_streamlit_pages.py::test_pages_render_without_exceptions -vv
  ```

- 实际结果：`任务记录` 渲染抛出 `KeyError: <TaskStatus.OUTCOME_UNKNOWN: 'outcome_unknown'>`，页面无法打开。
- 期望结果：显示可理解的状态（例如“结果待核对”），页面继续渲染，并保留人工核对入口。
- 影响：真实任务进入供应商结果未知状态时，整个任务记录页不可用，属于交付前必须处理的问题。
- 修复结果：统一补齐 8 种合法任务状态的中文名称和详情徽标，并为未来未知状态增加“状态待确认”兜底；真实数据和回归测试均可正常渲染。

## 高优先级体验问题

### P2-001（已修复）：React 任务记录页直接展示原始状态值

- 页面：React `/tasks`「任务中心」
- 复现：打开 `/tasks`，查看第一条转写任务。
- 实际结果：状态显示为 `outcome_unknown`，普通用户会看到内部枚举值，无法理解下一步。
- 期望结果：显示业务中文，如“结果待核对/等待人工确认”，并提供对应处理动作或说明。
- 相关位置：[project/frontend/src/pages/TasksPage.tsx](project/frontend/src/pages/TasksPage.tsx) 第 10-16、65-70 行。
- 备注：这与 P1-001 属于同一类状态映射缺口，但分别影响 Streamlit 和 React 两个入口。
- 修复结果：`/tasks` 已显示“结果待核对”，页面正文中不再出现 `outcome_unknown`；新增独立页面测试覆盖该状态。

## 自动化测试结果

| 检查项 | 结果 |
| --- | --- |
| 后端全量 pytest | 全部通过 |
| Streamlit 任务页回归 | 6 项通过，包含真实 `OUTCOME_UNKNOWN` 数据和选中详情 |
| 前端 Vitest | 17 个测试文件、132 项通过 |
| Ruff（任务页及对应测试） | 通过 |
| TypeScript `tsc --noEmit` | 通过 |
| Vite production build | 通过；有 antd 产物大于 500KB 的构建警告 |

## 浏览器按钮巡检

已在实际运行页面加载并巡检以下路由：

`/dashboard`、`/studio`、`/candidates`、`/pipeline`、`/production`、`/feedback`、`/transcription`、`/tasks`、`/admin`、`/analytics`、`/ai-copy`、`/publish`、`/avatar`、`/video-editor`、`/crawler`、`/help`。

已安全点击并观察结果的控件包括：

- 任务队列、任务记录、系统设置、深度分析的刷新按钮；
- 智能创作「修改设置」；
- 转写「新建转写」「转写历史」；
- AI 文案「历史记录」；
- 爬虫「历史记录」；
- 帮助中心「打开系统设置」；
- 反馈页空表单「保存反馈」校验；
- 智能创作入口「马上就好」的任务续接跳转。

浏览器日志未发现业务运行时异常；发现的主要是 React Router 升级提示、antd `bodyStyle`/`rc-collapse children` 弃用提示，暂未导致页面失败。

### 覆盖边界（重要）

本轮重新盘点后，16 个路由当前实际渲染约 **300 个按钮实例**（其中包含导航、表格逐行按钮和时间轴按钮）。因此，上一版报告不能理解为“300 个按钮全部点击通过”。当前覆盖情况是：

- 已实际点击：刷新、设置弹窗、历史/新建切换、候选搜索、转写交接、空表单校验、管理表单/确认弹窗入口、剪辑任务历史/高级设置、发布中心「选择成片」步骤等非破坏性按钮。
- 已验证状态但未提交：AI 文案改写按钮因无原稿而禁用；爬虫「找素材」因各平台待登录而禁用；发布中心「去选择成片」因账号未完成配置而禁用；数字人生成按钮在无口播文案时返回校验提示。
- 只到确认前、没有确认：生成激活码、客户充值、禁用客户、充值审批、重置管理员密码、开始创作、开始付费改写、真实发布、删除/下载等按钮。
- 仍未逐一点击：候选素材每一行的「文案转写」、任务队列每一行的审核/详情、转写时间轴每一段、数字人历史任务的播放/下载、剪辑时间轴控制和帮助折叠等重复或高副作用按钮。

所以当前结论是“核心安全路径已抽测，危险路径已验证到确认边界”，不是“所有按钮逻辑均已通过”。

## 子界面级巡检补充

本轮又展开了以下内部界面，确认了入口、状态和返回边界：

- 系统级：积分余额浮层、积分流水入口、个人中心、侧边栏展开/折叠、高级工具子导航；
- 智能创作：创作设置、素材网站管理、发布账号管理、出镜人设置；“保存并开始创作”在素材网站不可用时保持禁用；
- 数字人：形象库、声音库、产品讲解、历史任务；上传训练素材、产品主图和开始合成均未提交；
- 文案：AI 文案历史抽屉及载入入口；全部删除和单条删除未执行；
- 爬虫：历史记录抽屉、历史批次查看、结果详情、原文案入口；查看原文案会进入转写复核页；
- 转写：结果待确认状态、更多操作菜单、导出入口；当前 `OUTCOME_UNKNOWN` 任务的保存/确认按钮正确保持禁用；
- 发布：账号配置、平台切换、选择成片、发布内容复核；确认发布按钮因账号未配置而禁用，未提交；
- 帮助：四个 FAQ 分类标签均可展开，内容能够切换；快捷键搜索/帮助入口尚未完成真实键盘事件验收。

### 子界面仍需专项验收

- 登录/扫码、验证码和外部浏览器窗口；
- 文件选择器、训练上传、产品图片上传；
- AI 文案“开始去重改写”、云转写、数字人生成、云剪辑下载；
- 发布账号真实登录、平台发布确认和平台结果回填；
- 删除、批量删除、充值审批、客户充值、禁用客户、管理员密码重置；
- 全局搜索（Ctrl+K）和快捷键帮助（Ctrl+/）的真实键盘事件。

以上项目不是漏测后被标记为通过，而是因为会产生费用、外部登录、文件写入或不可逆数据变化，必须准备专用测试数据和明确回滚方案后单独验收。

## 未执行项（避免产生真实业务副作用）

- 真实付费的 AI 文案、云转写、数字人制作；
- 真实平台发布、账号扫码登录及验证码流程；
- 充值审批、生成激活码、充值、禁用、删除、重置密码；
- 会写入新任务或启动外部爬取的主提交按钮。

这些按钮需要单独准备测试账号、额度和回滚方案后再做验收，不应与本轮无副作用巡检混在一起。

## 上线前发布审计补充（第二轮）

### 当前不能宣称“全系统通过”的原因

- 后端实际注册了发布、充值、爬虫、转写、数字人、剪辑、通知、模板等大量 API；本轮只对可安全读取的页面状态和确认前流程做了验证。
- Admin、Analytics、Dashboard、Feedback、Help、Studio 和 Tasks 均已补最低页面回归；复杂子界面仍需结合实际运行页巡检。
- 全局搜索、快捷键帮助、扫码/验证码、文件选择器、外部浏览器窗口无法仅靠当前登录状态完成真实验收。
- 当前工作区仍有大量 Git 变更；上线门禁会阻止脏工作区交付，但仍需把确认后的上线范围形成一个提交。

### 自动化与静态检查

- 后端全量 pytest：全部通过，原 P1-001 已由真实 `OUTCOME_UNKNOWN` 数据回归覆盖。
- 前端 Vitest：17 个测试文件、132 项通过；新增 6 个主要页面的空数据/登录门/兼容跳转回归。
- 核心产品代码 Ruff：`app_pages project/backend src tests` 通过。
- 全仓 Ruff：通过；`forge_rss_analysis` 已确认是第三方分析参考副本，产品无依赖，现已从 Git 和交付范围排除。
- TypeScript 检查和 Vite 构建：通过；构建仍有 antd 产物超过 500KB 的警告。
- 未带认证访问业务 API 均返回 401，说明认证拦截存在；本轮没有伪造或绕过登录继续调用业务接口。

### 新增上线风险分级

- **P1-002（已建立硬门禁，待形成提交）发布基线不可复现**：`scripts/check_release.ps1` 会在工作区不干净时直接失败，并阻止密钥、数据库、媒体、运行数据和第三方参考副本进入交付；当前剩余动作是审核并提交本批预期改动。
- **P2-002（已缓解）页面测试覆盖缺口**：新增 Admin、Analytics、Dashboard、Feedback、Help、Studio 六个页面的最低回归，Tasks 状态回归已单独覆盖；真实外部窗口和高副作用子界面仍保留专项验收。
- **P2-003（已修复）全仓静态检查不通过**：第三方 ForgeRSS 副本已明确排除，示例代码 Ruff 错误已修复，全仓 Ruff 通过。
- **观察项**：从任务队列进入某个生产详情时，页面会先短暂显示“马上就好”加载态，约数秒后才出现真实任务详情；慢网络下需要确认是否有足够明确的加载提示。

## 建议交付顺序

1. 已完成：修复 P1-001、P2-001，并补齐 Streamlit/React 的 `OUTCOME_UNKNOWN` 回归测试；
2. 已完成：建立上线门禁、交付范围文档，排除 `forge_rss_analysis`，并补主要页面最低回归；
3. 待确认：审核当前大量改动和 7 个图片删除，形成干净提交后无参数运行上线门禁；
4. 最后按“付费/发布/充值审批/外部登录”专项测试表做一次带确认的真实链路验收。
