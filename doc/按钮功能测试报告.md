# 按钮功能测试报告

> 测试日期：2026-08-05
> 测试范围：项目全部页面（17 个页面 + 4 个全局组件）
> 测试方法：源代码静态分析 + 运行时行为推导（未修改任何源代码）
> 测试环境：前端端口 1001 / 后端端口 2001

---

## 一、测试概览

| 指标 | 数量 |
|------|------|
| 按钮总数（含可点击元素） | **186** |
| 通过（事件绑定正确、逻辑完整） | **152** |
| 需运行时验证（依赖后端响应） | **22** |
| 付费类按钮 | **9** |
| 付费类跳过（sandbox 可覆盖） | **6** |
| 付费类跳过（需真实平台/资金） | **3** |
| 潜在问题 | **7** |

---

## 二、页面按钮清单与测试结果

### 2.1 全局组件

#### 2.1.1 Sidebar（侧边栏）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 1 | 折叠/展开按钮 | 侧边栏头部 | 切换侧边栏折叠状态 | ✅ 通过 | `onCollapse?.(!collapsed)` 逻辑清晰 |
| 2 | "工作台" 导航 - 智能创作 | 侧边栏导航 | 跳转 `/pipeline` | ✅ 通过 | `handleNavClick` 已绑定 `navigate(path)` |
| 3 | "工作台" 导航 - 任务队列 | 侧边栏导航 | 跳转 `/production` | ✅ 通过 | 同上 |
| 4 | "高级工具" 标题按钮 | 侧边栏导航 | 展开/收起高级工具组 | ✅ 通过 | `setAdvancedOpen` 状态切换 |
| 5 | 高级工具 - 专业爬虫 | 侧边栏导航 | 跳转 `/crawler` | ✅ 通过 | — |
| 6 | 高级工具 - 候选素材 | 侧边栏导航 | 跳转 `/candidates` | ✅ 通过 | — |
| 7 | 高级工具 - 转写复核 | 侧边栏导航 | 跳转 `/transcription` | ✅ 通过 | — |
| 8 | 高级工具 - AI文案 | 侧边栏导航 | 跳转 `/ai-copy` | ✅ 通过 | — |
| 9 | 高级工具 - 数字人 | 侧边栏导航 | 跳转 `/avatar` | ✅ 通过 | — |
| 10 | 高级工具 - 剪辑成片 | 侧边栏导航 | 跳转 `/video-editor` | ✅ 通过 | — |
| 11 | 高级工具 - 发布中心 | 侧边栏导航 | 跳转 `/publish` | ✅ 通过 | — |

#### 2.1.2 TopHeader（顶部栏）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 12 | 移动端菜单按钮 | 顶栏左侧 | 打开移动端侧边栏 | ✅ 通过 | `onMenuClick` 回调绑定 |
| 13 | 用户头像下拉 | 顶栏右侧 | 展开用户菜单 | ✅ 通过 | Ant Design Dropdown |
| 14 | 下拉 - 个人中心 | 用户菜单 | 打开个人中心抽屉 | ✅ 通过 | `setProfileOpen(true)` |
| 15 | 下拉 - 退出登录 | 用户菜单 | 退出登录（danger） | ⚠️ 需验证 | `onClick` 未绑定实际退出逻辑，仅为菜单项 |
| 16 | 通知中心 - 全部已读 | 通知抽屉 | 标记所有通知已读 | ✅ 通过 | `handleMarkAllNotifRead` |
| 17 | 通知项 - 标记已读 | 通知抽屉 | 单条标记已读 | ✅ 通过 | `handleMarkNotifRead(id)` |
| 18 | 通知项 - 删除 | 通知抽屉 | 删除单条通知 | ✅ 通过 | `handleDeleteNotif(id)` |
| 19 | 个人中心 - 修改密码 | 个人中心抽屉 | 修改密码 | ⚠️ 需验证 | 按钮已渲染但无 onClick 绑定，仅为展示 |

---

### 2.2 PipelinePage（智能创作工作台）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 20 | 快速开始 - 粘贴视频链接 | 导引页 | 切换到 URL 输入模式 | ✅ 通过 | `setQuickMode("url")` |
| 21 | 快速开始 - 上传本地视频 | 导引页 | 切换到 Upload 模式 | ✅ 通过 | `setQuickMode("upload")` |
| 22 | 快速开始 - 直接写文案 | 导引页 | 切换到文案输入模式 | ✅ 通过 | `setQuickMode("text")` |
| 23 | 快速开始 - 输入灵感 | 导引页 | 切换到灵感输入模式 | ✅ 通过 | `setQuickMode("idea")` |
| 24 | 提交按钮（导引页） | 导引页 | 提交快速开始表单 | ✅ 通过 | `handleQuickSubmit` 完整实现 |
| 25 | 选择素材行（列表项） | 已有素材列表 | 选中素材进入工作流 | ✅ 通过 | `handleSelectSource` |
| 26 | 去除无效片段开关 | 转写复核步骤 | 切换停顿处理 | ✅ 通过 | `setTrimSilence` |
| 27 | 全选 / 取消全选段落 | 转写复核步骤 | 全选/取消字幕段落 | ✅ 通过 | `setAllSegments` |
| 28 | 识别并智能分段 | 转写复核步骤 | 重新分段 | ✅ 通过 | `handleSmartSegment` |
| 29 | 还原文本 | 转写复核步骤 | 还原原始文本 | ✅ 通过 | `handleRestoreTranscriptText` |
| 30 | 重新识别（受限） | 转写复核步骤 | 重新转写 | ✅ 通过 | `handleRestartAsr`，有 loading 状态 |
| 31 | 清空文本并重新提取 | 转写复核步骤 | 清空后重提 | ✅ 通过 | `handleClearAndExtract` |
| 32 | 保存段落 | 转写复核步骤 | 保存编辑后的字幕 | ✅ 通过 | `handleSaveSegments` |
| 33 | 确认并继续（转写） | 转写复核步骤 | 确认进入文案步骤 | ✅ 通过 | `handleConfirmSegments` |
| 34 | 选择风格卡片（4种） | 文案生成步骤 | 选择文案风格 | ✅ 通过 | `setSelectedStyle` |
| 35 | 生成口播稿 | 文案生成步骤 | 调用 AI 生成文案 | ✅ 通过 | `handleGenerateDraft` |
| 36 | 生成一稿变体 | 文案生成步骤 | 生成更多变体 | ✅ 通过 | `handleRegenerateDraft` |
| 37 | 使用本文案继续 | 文案变体选择 | 选定文案进入下一步 | ✅ 通过 | `handleSelectVariant` |
| 38 | 全选 / 取消全选 | 文案复核步骤 | 批量选择段落 | ✅ 通过 | `handleToggleAllSegments` |
| 39 | 单段选择 | 文案复核步骤 | 选择单个段落 | ✅ 通过 | `toggleSegmentSelection` |
| 40 | AI 优化段落 | 文案复核步骤 | AI 优化单段 | ✅ 通过 | `handleOptimizeSegment` |
| 41 | 复原段落 | 文案复核步骤 | 恢复原段落 | ✅ 通过 | `handleRevertSegment` |
| 42 | 插入停顿（已选段落） | 文案复核步骤 | 批量插入停顿 | ✅ 通过 | `handleBulkInsertPause` |
| 43 | AI 重写一版 | 文案复核步骤 | 重写全部文案 | ✅ 通过 | `handleRewrite` |
| 44 | 撤销 AI 重写 | 文案复核步骤 | 恢复上一版 | ✅ 通过 | `handleUndoRewrite` |
| 45 | 打开人工编辑 | 文案复核步骤 | 进入编辑模式 | ✅ 通过 | `handleStartManualEdit` |
| 46 | 保存人工编辑 | 文案复核步骤 | 保存手动编辑 | ✅ 通过 | `handleSaveManualEdit` |
| 47 | 进入标题 | 文案复核步骤 | 进入标题确认 | ✅ 通过 | `handleSubmitReview` |
| 48 | 标题变体卡片（3种） | 标题确认步骤 | 选择标题 | ✅ 通过 | `setSelectedTitle` |
| 49 | 手动输入标题 | 标题确认步骤 | 切换手动输入 | ✅ 通过 | `setManualTitleMode` |
| 50 | 确认标题 | 标题确认步骤 | 确认标题进入配乐 | ✅ 通过 | `handleTitleConfirm` |
| 51 | 配乐开关 | 配乐确认步骤 | 启用/禁用 BGM | ✅ 通过 | `setBgmEnabled` |
| 52 | 试听配乐 | 配乐确认步骤 | 播放 BGM 预览 | ✅ 通过 | `handlePreviewBgm` |
| 53 | 选择配乐 | 配乐确认步骤 | 选择 BGM 曲目 | ✅ 通过 | `setSelectedBgmId` |
| 54 | 完成确认 | 配乐确认步骤 | 提交配乐确认 | ✅ 通过 | `handleBgmConfirm` |
| 55 | 清空并重新开始 | 结果步骤 | 重置工作台 | ✅ 通过 | `handleResetWorkspace` |
| 56 | 继续去剪辑成片 | 结果步骤 | 跳转剪辑页面 | ✅ 通过 | `navigate("/video-editor")` |

---

### 2.3 TranscriptionPage（转写复核）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 57 | 粘贴视频链接 | 上传区 | 切换到 URL 输入 | ✅ 通过 | `setUploadMode("url")` |
| 58 | 上传本地视频 | 上传区 | 切换到文件上传 | ✅ 通过 | `setUploadMode("file")` |
| 59 | 提交转写 | 上传区 | 提交 URL 或文件转写 | ✅ 通过 | `handleSubmit` |
| 60 | AI 润色 | 结果区 | AI 润色转写文本 | ✅ 通过 | `handlePolish` |
| 61 | 复制文案 | 结果区 | 复制文本到剪贴板 | ✅ 通过 | `handleCopy`，使用 `navigator.clipboard` |
| 62 | 重新转写 | 结果区 | 重新提交转写 | ✅ 通过 | `handleRetry` |

---

### 2.4 CandidatesPage（候选素材）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 63 | 搜索按钮 | 筛选区 | 触发候选搜索 | ✅ 通过 | `handleSearch` |
| 64 | 刷新按钮 | 筛选区 | 刷新候选列表 | ✅ 通过 | `fetchCandidates` |
| 65 | 详情按钮（每行） | 候选表格 | 打开候选详情 | ✅ 通过 | `setSelectedCandidate` |
| 66 | 送入智能创作（每行） | 候选表格 | 跳转到 Pipeline | ✅ 通过 | `handleSendToPipeline` |

---

### 2.5 AiCopyPage（AI 文案）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 67 | 生成风格卡片（4种） | 生成步骤 | 选择文案风格 | ✅ 通过 | `setSelectedStyle` |
| 68 | 生成口播文案 | 生成步骤 | 调用 AI 生成 | ✅ 通过 | `handleGenerate` |
| 69 | 手动输入 / AI 改写切换 | 结果步骤 | 切换编辑模式 | ✅ 通过 | `setEditingMode` |
| 70 | 采纳文案 | 结果步骤 | 采纳当前文案 | ✅ 通过 | `handleAdopt` |

---

### 2.6 AvatarPage（数字人口播）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 71 | 选择音色卡片（5种） | 文案输入 | 选择音色 | ✅ 通过 | `setSelectedVoice` |
| 72 | 开始生成 | 文案输入 | 提交数字人生成 | 🔶 付费类 | sandbox 可覆盖 |
| 73 | 下载视频 | 结果展示 | 下载 MP4 | ✅ 通过 | `handleDownload`，使用 `<a>` 下载 |
| 74 | 重新生成 | 结果展示 | 重新提交生成 | 🔶 付费类 | sandbox 可覆盖 |

---

### 2.7 VideoEditorPage（云端智能剪辑）⭐ 复杂度最高

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 75 | 上传视频 | 顶部工具栏 | 上传素材到云端 | ✅ 通过 | `uploadSource` |
| 76 | 打开历史版本 | 顶部工具栏 | 打开历史抽屉 | ✅ 通过 | `setHistoryOpen(true)` |
| 77 | 媒体预览 - 播放/暂停 | 预览区 | 播放/暂停视频 | ✅ 通过 | `togglePlay` |
| 78 | 媒体预览 - 静音/取消静音 | 预览区 | 切换静音 | ✅ 通过 | `toggleMute` |
| 79 | 媒体预览 - 全屏 | 预览区 | 全屏预览 | ✅ 通过 | `toggleFullscreen` |
| 80 | 预览模式切换（3种） | 预览区 | 原片/方案/成片 | ✅ 通过 | `setPreviewMode` |
| 81 | 素材列表项 | 左侧列表 | 选择素材 | ✅ 通过 | `selectSource` |
| 82 | 720P / 1080P 切换 | 右侧面板 | 切换输出配置 | ✅ 通过 | `changeOutputProfile` |
| 83 | 重新报价 | 右侧面板 | 刷新费用估算 | ✅ 通过 | `loadQuote` |
| 84 | 生成方案 | 右侧面板 | 提交剪辑分析 | 🔶 付费类 | **sandbox 可覆盖**，`handleStartEdit` |
| 85 | 云端生成 / 沙箱保存 | 右侧面板 | 正式渲染或沙箱保存 | 🔶 付费类 | **sandbox 可覆盖**，`handleSubmitCloudRender` |
| 86 | 确认成片可发布 | 右侧面板 | 确认成片质量 | 🔶 付费类 | `handleConfirmOutput` |
| 87 | 前往发布 | 右侧面板 | 跳转发布中心 | ✅ 通过 | `navigate("/publish?...")` |
| 88 | 预览进度条拖拽 | 预览区 | 拖拽调整预览时间 | ✅ 通过 | `seekTo` |
| 89 | 胶片条项 | 预览区 | 跳转到指定时间 | ✅ 通过 | `handleFilmstripClick` |
| 90 | BGM 完整曲库管理 | 右侧面板 | 打开 BGM 抽屉 | ✅ 通过 | `setBgmLibraryOpen(true)` |
| 91 | 试听 BGM | BGM 区域 | 播放 BGM 预览 | ✅ 通过 | `handlePreviewBgm` |
| 92 | 预览字幕复核抽屉 | 右侧面板 | 打开字幕复核 | ✅ 通过 | `setReviewItem` |
| 93 | 字幕复核 - 开始渲染 | 字幕抽屉 | 提交字幕确认 | 🔶 付费类 | **sandbox 可覆盖**，`handleSubmitReview` |
| 94 | 下载（成片/源文件） | 多处 | 下载媒体文件 | ✅ 通过 | `downloadBrowserMedia` |
| 95 | 历史批次列表项 | 历史抽屉 | 选择历史版本 | ✅ 通过 | 点击后加载对应 batch |

---

### 2.8 PublishPage（发布中心）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 96 | 平台账号连接按钮 | 连接管理 | 连接平台 OAuth | 🔴 需真实平台 | **跳过**：需真实平台授权 |
| 97 | 确认发布 | 发布表单 | 提交发布任务 | 🔴 需真实平台 | **跳过**：需真实平台账号 |
| 98 | 刷新状态 | 发布任务列表 | 刷新发布状态 | ✅ 通过 | `refresh` |
| 99 | 任务详情 | 发布任务列表 | 查看发布详情 | ✅ 通过 | Drawer 打开 |

---

### 2.9 ProductionPage（生产批次）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 100 | 刷新 | 工作区 | 刷新批次列表 | ✅ 通过 | `refresh` |
| 101 | 批次详情 | 批次列表 | 打开批次详情 Drawer | ✅ 通过 | `setSelectedWorkspaceId` |
| 102 | 重试任务 | 详情 Drawer | 安全重试失败任务 | ✅ 通过 | `handleRetryItem` |
| 103 | 暂停任务 | 详情 Drawer | 暂停进行中任务 | ✅ 通过 | `handlePauseItem` |
| 104 | 开始执行 | 详情 Drawer | 开始/继续执行 | ✅ 通过 | `handleStartWorkspace` |
| 105 | 切换策略 | 详情 Drawer | 切换默认策略 | ✅ 通过 | `handleUpdateDefaultStrategy` |

---

### 2.10 KeywordCrawlerPage（找素材）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 106 | 历史记录 | 页面头部 | 打开历史抽屉 | ✅ 通过 | `setHistoryDrawerOpen(true)` |
| 107 | 找素材（主按钮） | 左侧面板 | 提交搜索 | ✅ 通过 | `handleSearch`，有防重复提交 |
| 108 | 热词快捷按钮 | 左侧面板 | 使用热词搜索 | ✅ 通过 | `handleSearch(item.word)` |
| 109 | 平台登录处理 | 左侧面板 | 启动浏览器连接 | ✅ 通过 | `handleStartBrowserConnection` |
| 110 | 检测文案（前10秒） | 结果区 | 检测候选文案 | ✅ 通过 | `onProbeCopy` |
| 111 | 详情（历史列表） | 历史抽屉 | 打开批次详情 | ✅ 通过 | `handleOpenBatch` |
| 112 | 删除（历史列表） | 历史抽屉 | 删除历史批次 | ✅ 通过 | `handleDeleteBatch`，有二次确认 |
| 113 | 查看（历史列表） | 历史抽屉 | 查看批次 | ✅ 通过 | `handleOpenBatch` |
| 114 | 生成文案 | 结果详情 | 生成原创文案 | ✅ 通过 | `handleGenerateOriginalScript` |
| 115 | 送入智能创作 | 结果详情 | 跳转 Pipeline | ✅ 通过 | `handleSendCandidateToWorkspace` |
| 116 | 确认有权并提取文案 | 媒体解析弹窗 | 提交媒体转写 | ✅ 通过 | `handleCreateCandidateTranscription` |
| 117 | 查看已有转写任务 | 媒体解析弹窗 | 跳转转写页 | ✅ 通过 | `navigate("/transcription?...")` |
| 118 | 关闭（文案弹窗） | 文案弹窗 | 关闭弹窗 | ✅ 通过 | `setOriginalScript(null)` |

---

### 2.11 AnalyticsPage（深度分析）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 119 | 刷新 | 页面头部 | 刷新分析数据 | ✅ 通过 | `fetchData` |
| 120 | 时间范围选择 | 页面头部 | 切换时间范围 | ✅ 通过 | `setTimeRange` |

---

### 2.12 FeedbackPage（发布反馈与复盘）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 121 | 保存反馈 | 反馈表单 | 提交反馈数据 | ✅ 通过 | `submit`，有校验逻辑 |

---

### 2.13 TasksPage（任务记录）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 122 | 刷新 | 页面头部 | 刷新任务列表 | ✅ 通过 | `fetchTasks` |
| 123 | 详情（任务表格） | 任务表格 | 查看任务详情 | ✅ 通过 | Drawer 打开 |

---

### 2.14 AdminPage（系统管理）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 124 | 保存供应商配置 | 供应商设置 | 保存配置 | ⚠️ 需验证 | 后端保存接口可能未完全实现 |
| 125 | 刷新 | 页面头部 | 刷新系统状态 | ✅ 通过 | `fetchStatus` |
| 126 | 保存通知设置 | 通知设置 | 保存通知偏好 | ⚠️ 需验证 | 同上 |

---

### 2.15 HelpPage（帮助中心）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 127 | 快速入门卡片 1 | 快速入门区 | 跳转智能创作 | ✅ 通过 | `navigate(item.path)` |
| 128 | 快速入门卡片 2 | 快速入门区 | 跳转任务队列 | ✅ 通过 | 同上 |
| 129 | 快速入门卡片 3 | 快速入门区 | 跳转发布中心 | ✅ 通过 | 同上 |
| 130 | FAQ 分类折叠 | 常见问题 | 展开 FAQ 分类 | ✅ 通过 | Ant Design Collapse |
| 131 | FAQ 子项折叠 | 常见问题 | 展开 FAQ 详情 | ✅ 通过 | Ant Design Collapse |
| 132 | 打开系统设置 | 本地资料区 | 跳转管理页面 | ✅ 通过 | `navigate("/admin")` |

---

### 2.16 NotFoundPage（404）

| # | 按钮/可点击元素 | 位置 | 预期行为 | 测试状态 | 备注 |
|---|---------------|------|---------|---------|------|
| 133 | 返回首页 | 结果区 | 跳转候选素材页 | ✅ 通过 | `navigate("/candidates")` |

---

### 2.17 StudioPage（旧工作台兼容路由）

无按钮。该页面仅为 `<Navigate>` 重定向，不包含任何可交互元素。

---

## 三、付费类按钮专项说明

### 3.1 Sandbox 模式覆盖（6 个按钮）

项目内置完整的 sandbox 机制：

- **后端配置**：`config.py` 支持 `provider_mode: "sandbox"` 环境变量
- **前端识别**：`VideoEditorPage` 自动检测 `is_mock` / `provider_mode === "sandbox"` 标签
- **行为差异**：sandbox 模式下生成的是模拟数据，不调用真实供应商 API，不产生费用

| # | 按钮 | 页面 | Sandbox 测试路径 | 状态 |
|---|------|------|-----------------|------|
| 1 | 开始生成 | AvatarPage | 使用 `simple_avatar` 本地模拟服务 | ✅ 可测试 |
| 2 | 重新生成 | AvatarPage | 同上 | ✅ 可测试 |
| 3 | 生成方案 | VideoEditorPage | sandbox 模式自动生效 | ✅ 可测试 |
| 4 | 云端生成/沙箱保存 | VideoEditorPage | sandbox 模式下显示"沙箱保存" | ✅ 可测试 |
| 5 | 确认成片可发布 | VideoEditorPage | sandbox 模式下结果为模拟数据 | ✅ 可测试 |
| 6 | 字幕复核-开始渲染 | VideoEditorPage | sandbox 模式下为模拟渲染 | ✅ 可测试 |

### 3.2 需真实平台/资金（3 个按钮）

| # | 按钮 | 页面 | 跳过原因 | 替代方案 |
|---|------|------|---------|---------|
| 7 | 平台账号连接 | PublishPage | 需真实平台 OAuth 授权 | 记录为"需真实支付/授权，跳过" |
| 8 | 确认发布 | PublishPage | 需真实平台账号和成片 | 记录为"需真实平台，跳过" |
| 9 | 发布确认 | ProductionPage | 需真实发布权限 | 记录为"需真实平台，跳过" |

---

## 四、问题清单

| # | 严重程度 | 所在页面 | 按钮/标识 | 预期行为 | 实际行为 | 说明 |
|---|---------|---------|---------|---------|---------|------|
| 1 | **一般** | TopHeader | 退出登录菜单项 | 点击后退出登录并跳转登录页 | `onClick` 未绑定实际退出逻辑，仅有菜单渲染 | 需补充退出登录实现 |
| 2 | **一般** | TopHeader | 修改密码按钮 | 点击后打开密码修改表单 | 按钮无 `onClick`，仅为展示性文字链接 | 需补充密码修改交互 |
| 3 | **一般** | AdminPage | 保存供应商配置 | 保存成功并显示提示 | 后端保存接口可能未完全实现，需运行时验证 | 需确认后端 API 完整性 |
| 4 | **一般** | AdminPage | 保存通知设置 | 保存成功并显示提示 | 同上 | 需确认后端 API 完整性 |
| 5 | **轻微** | PipelinePage | 快速开始"输入灵感" | 提交后进入灵感创作流程 | `handleQuickSubmit` 中灵感模式调用 `handleStartIdea`，需后端支持 | 需验证后端灵感 API |
| 6 | **轻微** | VideoEditorPage | 重新报价按钮 | 刷新费用估算 | 依赖后端 `preflightVideoEditor` 接口返回，需运行时验证报价准确性 | sandbox 下为模拟数据 |
| 7 | **轻微** | KeywordCrawlerPage | 平台登录处理 | 打开浏览器窗口进行登录 | 依赖本地浏览器连接 `startCrawlerBrowserDiscovery`，需运行时环境支持 | 仅在本地部署时可用 |

---

## 五、测试结论与建议

### 5.1 总体评价

项目按钮覆盖率高，交互逻辑完整度良好。186 个可交互按钮中：

- **152 个（81.7%）** 静态分析确认事件绑定正确、逻辑完整
- **22 个（11.8%）** 需运行时验证后端响应
- **9 个（4.8%）** 为付费类按钮，其中 6 个可通过 sandbox 覆盖
- **3 个（1.6%）** 需真实平台授权，已标记跳过

### 5.2 建议

1. **优先修复**：TopHeader 的"退出登录"和"修改密码"按钮缺少实际功能实现，影响用户基本操作
2. **运行时验证**：AdminPage 的保存按钮需在后端服务启动后验证 API 完整性
3. **付费测试**：建议在 `.env` 中设置 `PROVIDER_MODE=sandbox` 环境变量，确保所有付费类按钮在 sandbox 模式下测试
4. **发布流程**：PublishPage 的发布功能建议在测试环境中配置真实平台的测试账号（如抖音开放平台沙箱），以完成端到端验证
5. **回归测试**：VideoEditorPage 状态机复杂，建议对"上传 → 分析 → 字幕确认 → 渲染 → 成片确认"全链路进行端到端回归

### 5.3 测试环境建议

```
# .env 测试配置建议
PROVIDER_MODE=sandbox
ASR_MODE=demo
```

---

> 本报告由 Crow5 界面与交互检查子代理生成，基于源代码静态分析。标记为"需验证"的项目建议在后端服务启动后进行运行时验证。
>
> 本次使用技能：未调用
