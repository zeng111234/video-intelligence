# 账号安全使用说明（防封）

本系统的采集与发布都使用**独立的专用浏览器环境**登录，与你的日常浏览器完全隔离。
下面的做法能最大限度降低账号风险。

## 1. 用什么账号登录

- **强烈建议用专门的"小号"登录采集和发布**，不要用主账号。
  万一平台风控误判，损失的是小号，不影响你的主账号和日常使用。
- 登录窗口打开后，系统会提示"建议使用专门的采集小号"。直接扫码登录小号即可。

## 2. 换号方法

每个平台只有一个专用浏览器环境（保存在 data/browser_profiles 和
%LOCALAPPDATA%/VideoInsight/publish_profiles 下）。换号步骤：

1. 打开对应平台的登录窗口（采集页或发布中心点"打开登录窗口"）
2. 在窗口里点击账号头像 -> 退出登录
3. 扫码登录新账号（小号）
4. 刷新页面状态，确认显示"已登录"

## 3. 系统自带的防封保护

| 保护项 | 默认值 | 在哪里调 |
|---|---|---|
| 采集：每平台每天最多真实采集 | 8 次 | .env 的 BROWSER_MAX_REAL_RUNS_PER_WINDOW |
| 采集：同平台两次采集最小间隔 | 60 分钟 | 内置固定 |
| 采集：遇到验证码/访问频繁 | 自动暂停 24 小时 | 内置 |
| 发布：每平台每天最多发布 | 5 条 | .env 的 PUBLISH_DAILY_LIMIT |
| 发布：两条发布最小间隔 | 15 分钟 | .env 的 PUBLISH_MIN_INTERVAL_MINUTES |
| 发布：连续 2 次失败或平台提示"发布频繁" | 账号自动暂停 24 小时 | 发布中心可手动恢复 |

发布暂停后，发布中心顶部会显示黄色提示和"立即恢复"按钮；也可以等 24 小时自动解除。

## 4. 想更稳的建议

- 采集频率保持低频（每天几次），比任何伪装都有效
- 发布前在页面确认内容没问题再确认发布
- 不要同时用多台机器操作同一个账号

## 5. 积分与训练收费

- 新账户默认赠送 **400 积分**（1 积分 = 1 元），可在 .env 的 DEFAULT_CREDIT_BALANCE 调整
- 数字人训练收费：**声音训练 60 积分**、**云形象（脸部）训练 100 积分**
  （.env 的 AVATAR_VOICE_TRAINING_CREDITS / AVATAR_FACE_TRAINING_CREDITS 可调）
- 训练提交时扣积分，余额不足会提示先充值；训练提交后失败不退（与数字人生成一致）

## 6. 客户激活码（给客户用）

- 客户打开系统会看到"激活码登录页"，输入你发的激活码即可进入，无需注册
- 每个激活码 = 一个客户，积分账户独立；客户费用从自己账户扣，余额不足会被阻止
- 登录凭证 12 小时有效，过期后重新输入激活码即可

### 管理员操作（系统管理页 -> 客户激活码）

| 操作 | 说明 |
|---|---|
| 生成激活码 | 填客户名 + 初始赠送积分（默认 400）+ 数量，生成后直接复制发给客户 |
| 充值 / 扣减 | 客户列表点"充值"，输入金额即可（1 积分 = 1 元） |
| 禁用 / 启用 | 禁用后该客户立即无法登录 |
| 管理员账号 | 可创建多个管理员（各自登录管理），可重置密码 |

默认管理员账号 admin，密码在 .env 的 ADMIN_PASSWORD。
