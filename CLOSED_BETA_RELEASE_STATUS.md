# VideoInsight Windows 封闭内测交付状态

> **历史记录，已停止分发。** 本文记录的是 2026-08-09 的本机演示包，不再作为新电脑或新客户的安装指引。当前交付以服务器连接版的正式 HTTPS 与最终 EXE 验收结果为准。

更新时间：2026-08-09

## 本次应使用的安装包

- 文件：`project/frontend/release/VideoInsight-0.1.2-Setup.exe`
- 大小：292,828,160 字节（约 279.26 MiB）
- SHA256：`5732E918CA971075BBFED001C4EA71124AE7A904A16DF9F53D1B5B9FB718CB17`
- 内测激活码：`DEMO-0815`
- 内测积分：99,999（仅本机封闭内测数据，不是正式商业积分账户）

后续新分发不要再使用此前的 `0.1.0` 或 `0.1.1` 安装包。`0.1.2` 除了保留进程退出等待、一次重试、同目录版本切换与失败回滚，还会在隔离外部 Python/Node 的环境中自动启动和验收，不再要求客户输入 PowerShell 命令。

已经成功安装 `0.1.1` 的内测电脑不需要重新传包或升级：`0.1.1` 与 `0.1.2` 内的应用载荷完全一致（载荷 SHA256 均为 `DAFB209753778A810A7FC4DD6FD10ED1C63D58A2E84F4544EBAF73DB19F76471`）。`0.1.2` 只改进安装器的一键自动验收，供后续首次分发使用。

## 已在当前 Windows 电脑验证

- 安装、覆盖安装、卸载、重新安装均可执行。
- 桌面快捷方式、开始菜单快捷方式和“应用和功能”卸载项可用。
- 卸载程序不会删除 `%LOCALAPPDATA%\VideoInsight\data` 中的本机业务数据。
- 软件、后端服务及子进程均从安装目录运行。
- 在启动进程的 `PATH` 中移除 Python 和 Node 后，软件仍能启动并通过健康检查。
- `DEMO-0815` 可登录普通客户账号，显示 99,999 积分。
- 普通客户可访问任务和爬虫接口，不能访问管理员接口。
- 后端测试 214 项通过，前端测试 144 项通过；TypeScript、Vite 构建和 Ruff 检查通过。
- 安装包未包含 `.env`、数据库、日志、客户媒体或正式供应商私钥；封装运行时强制使用非计费沙箱配置。
- 本次验证未发起真实转写、数字人、成片或发布等付费供应商调用。

## 另一台干净 Windows 电脑仍必须验收

当前电脑已安装 Python 和 Node，因此“客户电脑从零安装”仍需在另一台 Windows 10/11 电脑上取得最终证据。

1. 复制 `project/frontend/release` 中的安装包和本说明到另一台电脑；验证脚本会由安装包写入程序目录。
2. 双击安装包并从桌面启动 VideoInsight。
3. 安装器会自动启动软件并执行 13 项检查，不需要输入命令。
4. 使用 `DEMO-0815` 登录查看页面。
5. 保留桌面生成的 `VideoInsight-clean-pc-acceptance-*.txt`。只有全部显示 `PASS`，才能声明“干净电脑点击即用”已验收。

## 已知外部限制

- 安装包目前未进行 Windows 代码签名，首次运行可能触发 SmartScreen；这不等于安装包损坏。
- 当前使用 Electron 默认图标，属于可见的品牌完成度问题，但不阻断封闭内测。
- 安装包不分发正式供应商密钥。真实云转写、数字人、云成片和自动发布必须由公司端配置并在产生费用前确认。
- 未内置 FFmpeg/FFprobe。缺少时，剪辑能力降级为非计费演示模式；部分本地媒体校验不可用，但不会阻断应用启动。
- 网页平台采集和发布需要本机可用的 Edge/Chrome、平台登录状态及平台页面保持兼容。
- 手机自动化需要另行安装 ADB/Appium、连接并授权安卓设备；这不是普通客户默认路径。

## 2026-08-09 最终回归

- 实际安装版使用 `DEMO-0815` 登录成功，右上角显示 99,999 积分。
- 通过页面入口实际打开：智能创作、任务队列、专业爬虫、候选素材、转写复核、AI 文案、数字人、剪辑成片、发布中心和积分面板。
- 补查已注册子路由：数据概览、旧工作室跳转、反馈复盘、任务记录、深度分析、帮助中心、字幕旧路由跳转和管理员登录隔离。
- 页面巡检期间浏览器控制台无警告或错误；未提交搜索、充值、生成、登录平台或发布操作。
- 后端全套测试通过；Ruff 通过。
- 前端 TypeScript 检查通过，19 个测试文件共 144 项测试通过，Vite 生产构建通过。
- Vite 仍提示部分产物大于 500 kB；这是后续启动性能优化项，不阻断封闭内测。
- 浏览器截图接口不可用，已停止重复尝试；页面点击、路由、可见文本、控件状态和控制台验收均有实际运行证据。

## 当前结论

当前安装包已修复 PowerShell 解析失败和覆盖安装文件占用，并在隔离 Python/Node 的运行环境中完成 13 项自动检查。另一台电脑上的 `0.1.1` 已完成安装、启动、登录与权限验收，且其应用载荷与 `0.1.2` 完全一致。当前版本可以用于 8 月 14–15 日封闭演示，但不能对外宣称正式商用上线；正式供应商、平台账号、代码签名和自动更新仍需后续单独验收。
