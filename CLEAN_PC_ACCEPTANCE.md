# VideoInsight 客户电脑一键验收

目标：证明 Windows 10/11 64 位客户电脑不需要预装 Python、Node.js 或输入命令，即可安装、启动并连接公司的授权服务。自动验收不会运行爬虫、转写、文案、数字人、成片或发布，不会产生第三方费用。

## 客户只需要做两件事

1. 双击本次唯一发送的 `VideoInsight-<版本号>-Setup.exe`；不要再安装聊天记录里的旧包。
2. 看到“安装和检查完成”后，使用公司发放的真实激活码登录。

安装器会自动创建桌面和开始菜单入口、启动客户端并完成基础检查。不要把仓库、脚本、密钥或服务器 `.env` 发给客户。

覆盖安装会先保留旧程序，只有新版本自动验收全部通过后才清理旧程序。若启动、公司服务连接或自动验收失败，安装器会停止新版并恢复旧程序及原卸载信息；客户数据始终保留在 `%LOCALAPPDATA%\VideoInsight`。最终发送前仍要在内部干净电脑实际制造一次验收失败，确认旧版可以重新启动。

## 自动验收通过的含义

- 客户端文件、桌面快捷方式、卸载入口和本地数据目录正常。
- 内置后端能从安装目录启动，本机健康接口可用。
- 登录页可打开，公司激活服务可连接；无效激活码会被安全拒绝。
- 未登录时，任务、爬虫和管理接口都不能访问。
- 未登录时，已有转写、发布和智能创作后台队列不会继续调用云端；登录本机绑定客户后才恢复处理。
- 运行不依赖客户电脑已有的 Python 或 Node.js。

自动验收不代表真实供应商已经可用。正式试用前，还要在公司服务器完成供应商密钥配置，并由管理员在“公司服务状态”中确认；任何可能收费的动作仍需客户明确确认。

## 激活码与本机素材

- 这台电脑首次成功登录的客户账号会成为本机工作区主人；绑定文件只保存不可逆摘要，不保存激活码明文。
- 后续使用同一客户的激活码可以继续已有素材和任务。为防止不同客户互相看到本机文件，直接换成另一个客户激活码会被拒绝。
- 同一客户端可以同时保留普通客户和管理员会话，但普通业务只使用客户身份，管理接口只使用管理员身份；退出其中一个角色不会误退出另一个角色。
- 客户确实需要换账号、换公司或移交电脑时，不要让客户自行删除数据目录。先备份需要保留的素材和任务，再由服务人员执行迁移或清理；未完成迁移前不要重新发激活码反复尝试。

## 出现 Windows 安全提示

当前安装包若尚未购买 Windows 代码签名证书，SmartScreen 可能提示“Windows 已保护你的电脑”。先核对随包提供的 `SHA256SUMS.txt`；公开大规模销售前建议购买代码签名证书。不要让客户关闭杀毒软件。

## 更新

客户端启动后只检查公司 HTTPS 地址上的更新清单。发现更高版本时先询问用户，确认后才下载一次；清单中的版本和安装包文件名必须完全一致，下载文件必须通过大小和 SHA256 校验。同版本文件不能覆盖，更新清单不能回退；当前版本可继续使用时，更新服务临时不可用不会阻止启动。
