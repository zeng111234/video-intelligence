# VideoInsight 上线收尾报告（2026-08-10）

> 最新状态：本地整体验收已经继续完成，详见 `本地整体验收报告-2026-08-10.md`。最新证据包含 1,253 项 Python、155 项 React、真实 Chrome 14 个普通客户子页面、抖音官网 30/30 多列筛选，以及 Windows PowerShell 5 安装脚本兼容门禁。

## 当前结论

本地能够安全解决的代码、发布门禁、自动化测试、部署脚本、敏感信息隔离、真实免费爬虫和隔离双进程页面联调已经完成处理。当前版本已经具备“Windows 客户端 + 公司服务器控制层”的代码基础，并已证明服务器登录/积分与本机业务工作台可以同时工作，但还不能宣称正式商用上线。现有 `0.2.0` 目录版使用示例服务地址，已被新门禁判定为非正式包，不能发送；正式交付必须使用一个从未生成过的新版本号。

剩余事项主要依赖公司正式 HTTPS 服务器、真实账号、供应商密钥或负责人对最低成本付费验收的批准。它们已经在下文单独搁置，不会阻塞本地继续开发，也不会被误报为通过。

Goal 的本地可完成部分已收口；当前停在“等待公司正式服务器、域名、生产配置和付费验收批准”的明确位置。拿到这些条件后继续沿用同一个上线目标，不重新生成中间安装包。

## 本轮已解决

### 1. 发布门禁漏测和假失败

- `scripts/check_release.ps1` 原来只运行根目录测试，会漏掉新公司控制层测试；现在统一调用 `scripts/run_release_python_tests.ps1`。
- 门禁现在会单独运行桌面自动更新安全测试，不再只测 React 页面。
- 门禁原来强制覆盖测试 API Key，造成正常接口测试被 403 拦截；该错误环境覆盖已经删除。
- 门禁会从可用 Python 中选择真正具备 FastAPI、Streamlit、pandas、pytest 和性能插件的完整发布测试环境，不再误选仅用于桌面运行的精简 `.venv`。

### 2. 前端警告与测试假报错

- 补充 jsdom 的布局和媒体暂停兼容层，避免 Ant Design 在测试环境产生大量“浏览器未实现”假报错。
- 替换两个 Ant Design 已弃用的 `addonBefore` 用法。
- 两个核心长文本输入改为固定行数，消除测试中的 `NaN height` 警告，并保持页面布局稳定。
- React Router 已从 `6.30.4` 升级到 `7.18.2`，旧兼容选项已经删除；类型检查、155 项前端测试和正式构建均通过。

### 3. 现有“大树1”资产复用

- 公司服务器部署包会导入现有“大树1”形象和声音的供应商编号。
- 两项资产标记为公司共享资产，普通客户可见；客户私有训练资产仍按账号隔离。
- 已有克隆声音可以直接合成，不要求重新上传训练样本。
- 部署包不包含声音训练样本、供应商密钥、本机绝对路径、数据库、日志或媒体文件。
- 本轮没有调用真实供应商，没有重新训练，也没有产生供应商费用。

### 4. 发布和部署脚本

- 11 个 Windows PowerShell 发布、安装、更新和验收脚本语法通过。
- 6 个 Linux 服务器部署、备份、恢复和验收脚本语法通过。
- Docker Compose 结构检查通过；正式 `.env` 尚未创建，因此没有伪造生产密钥完成在线验收。
- Git 当前跟踪文件中，禁止进入交付的 `.env`、数据库、媒体、运行数据和第三方分析副本数量为 0。

### 5. 最终 EXE 防误发与密钥隔离

- Electron 在创建本地服务进程前会按名称（不区分大小写）删除全部供应商密钥；Python 启动器继续执行第二层清理，二者的禁止清单有自动一致性测试。
- 最终发布脚本只接受正式的非本机 HTTPS 公司服务，拒绝 `localhost`、环回地址、`.test`、`.invalid` 和 `example.com` 等示例域名。
- 打包前必须先通过公司服务无付费在线检查和干净工作区完整发布门禁；不会使用激活码、管理员密码或调用任何收费供应商。
- 目录版生成后会核对桌面配置、后端配置、版本号与正式域名完全一致，并扫描运行数据库、日志、媒体、私钥，以及进程环境、根 `.env`、生产 `.env` 和后端 `.env` 中已配置的供应商密钥；扫描只输出密钥名称和命中文件，不打印值。
- 同一版本安装包已经存在时会直接停止，避免覆盖旧包或再次向客户传输同名大文件。
- 本轮没有生成、覆盖或上传任何新安装包。

### 6. 生产服务器启动门禁与收费能力隔离

- 公司控制层在生产环境启动前会强制检查：接口文档关闭、会话使用 SQLite、正式域名和允许主机不是本机或示例地址、服务器爬虫保持沙箱模式。
- 云转写与云剪辑改为两个独立开关：`ASR_MODE=cloud` 只启用云转写，`VIDEO_EDITOR_PROVIDER_MODE=aliyun` 只启用云剪辑，避免配置一项时意外开放另一项收费能力。
- `ASR_MODE` 不是 `cloud` 时，新转写授权、上传和提交都会在扣积分前停止；已经提交过的任务仍可只查询和恢复，不会为了“再试一次”自动付费重提。
- 正式环境缺少阿里云工作空间、OSS 或访问密钥时会直接拒绝部署/启动，不会带着半套配置上线。

### 7. 幂等、重启和结果未知恢复

- 服务器重启前仍处于 `pending` 的收费操作会恢复为 `unknown`，并明确标记为不可自动重试，防止重启后重复提交或重复扣费。
- 长时间没有完成的 `pending` 操作也会按超时规则转为 `unknown`；近期仍在运行的操作不会被误判。
- 文案、转写、云剪辑和数字人仍由服务器保存权威幂等记录，客户端刷新、关闭或重开不能自行制造第二笔收费提交。

### 8. 备份和恢复收口

- 服务器数据目录默认权限为 `700`，备份文件为 `600`；备份 ZIP 本身不加密，因此文档已明确要求只进入公司加密备份存储，禁止放进 Git、聊天或公开网盘。
- 恢复前会完整校验归档路径、大小和 SQLite 数据库，并为正在运行的服务先生成一次恢复前快照。
- 恢复现在是“精确恢复”：旧的多余授权和资产文件会被清除，不再与备份叠加；所有旧登录会话会被撤销，避免恢复历史数据库后旧令牌重新生效。

### 9. 服务器连接版真实页面与本机客户隔离

- 在完全隔离的本机测试目录中分别启动公司控制层和桌面客户端；所有收费能力使用沙箱或无效测试地址，没有调用真实供应商。
- 普通客户使用服务器测试激活码登录后，桌面端显示服务器权威余额 `100 积分`，同时智能创作、任务、本地爬虫等本机业务接口正常加载；原先“只能查看余额和充值申请”的错误拦截已修复。
- 每个桌面运行目录会绑定首次成功登录的客户，只保存不可逆 SHA-256 摘要，不保存激活码。第二个测试客户在同一运行目录登录时返回 `409`，其上游临时会话也被立即撤销，避免看到前一客户的本机数据库和媒体。
- 同一浏览器在管理员与客户之间切换时，现在会删除上一个角色的媒体 Cookie；显式登录身份也优先于残留 Cookie，避免视频或音频预览误用另一个角色的本机工作区。该修复通过 18 项定向回归及完整发布门禁。
- 管理员页面真实加载了公司服务状态、测试激活码、服务器余额、管理员账号和折叠收费项目；普通客户仍不能调用管理接口。
- 页面真实显示并自动选中共享的“大树1”形象和声音，状态为已训练完成、无需重新训练。当时页面仍显示固定 45 秒的旧报价 `1.88 积分`，该报价后来确认有误，不能作为当前计费验收证据；现在应按文案预留、按供应商实际成片时长向上取整到整秒结算。本轮没有点击生成。
- 浏览器控制台没有业务错误。该证据证明本机双进程边界可用，但不替代正式 HTTPS、跨电脑、真实供应商和最终 EXE 验收。

### 10. 安装回滚、版本不可覆盖与部署指引修正

- 正式 Windows 构建不再提供 `0.2.0` 默认值，必须显式填写三段数字的新版本号，并拒绝 `0.2.0` 及更早版本。
- 离线安装包和服务器更新文件均拒绝覆盖同版本；已有更新清单时只接受更高版本，客户端也会拒绝清单版本与安装包文件名不一致。
- 覆盖安装现在把旧程序保留到新版启动及自动验收全部通过之后；失败时会停止新版、恢复旧程序和原卸载信息，首次安装失败则清理不完整程序，但不会删除客户数据。
- 安装器 PowerShell 脚本语法与 C# 引导程序编译通过，发布与更新定向回归通过；真实断网覆盖安装回滚仍必须在唯一最终 EXE 的内部干净电脑验收中完成。
- 公司服务器说明中“上传整个项目目录”的旧指令已删除；现在只允许上传显式版本号生成的控制层 ZIP。结构核验样本包含 171 个条目、禁止文件 0，并只带入训练完成的共享形象和声音编号。

### 11. 登录后后台任务、双角色计费身份与依赖安全

- 连接公司控制层时，转写、发布和智能创作后台队列只有在本机已经绑定且当前登录的是同一普通客户后才会领取任务。重启 EXE 后未登录时，已有队列会原样保留，不会匿名调用供应商或误报失败。
- 同一浏览器同时存在管理员和普通客户登录时，普通文案、转写、成片等业务请求只携带客户身份；只有管理后台、积分调整和管理员充值审批接口使用管理员身份。已修复普通 AI 文案可能被误转成管理员请求、从而绕过客户积分的风险。
- 管理员点击退出后会同时撤销公司控制层会话并清理本机管理员信息，不再只隐藏前端页面；某一角色收到 `401` 时也只清理该角色，不会把另一个仍有效的登录一起踢出。
- 登录代理现在校验公司服务返回的令牌、账号标识和角色，拒绝把管理员响应当成客户登录或反向混淆。
- 生产前端依赖审计为 0。Vitest、PostCSS 和 NanoID 已升级到安全补丁；开发工具仍保留 Vite 5 的 1 个高危、1 个中危本地开发服务器公告，因为安全修复要求跨主版本并会改变 Node 18 兼容边界。Vite 和 `node_modules` 不进入客户运行包，后续单独升级开发工具链。
- 本轮新增身份与退出逻辑已通过自动化测试、类型检查和正式构建；浏览器实页控制连接连续两次未建立，因此“双角色登录后点击退出”的新实页证据仍列为最终 EXE 验收项，不能把自动化结果写成按钮已人工点击通过。

## 自动化验证证据

| 检查 | 结果 |
| --- | --- |
| `scripts/check_release.ps1 -AllowDirty` | 早前通过；本次复验的首轮在前端测试环境销毁时出现一次异步清理偶发错误，单次重跑前端全套通过，余下桌面、类型和构建步骤单独补齐通过。正式提交后仍须无参数整条一次通过 |
| 公司控制层与 FastAPI 测试 | 299 项全部通过 |
| 服务层、适配器、性能基线、旧 Streamlit 页面 | 964 项全部通过 |
| 数字人按秒计费专项 | 后端 55 项、前端 22 项通过；“你好”报价 2 秒 / 0.09 积分；未提交付费任务 |
| React Vitest | 20 个文件、157 项通过 |
| 桌面启动隔离与自动更新安全测试 | 7 项通过 |
| TypeScript `tsc --noEmit` | 通过 |
| Vite 正式构建 | 通过；Ant Design 主包较大但不阻塞桌面交付 |
| PowerShell / Shell 脚本语法 | 11 / 6 全部通过 |
| Docker Compose 结构 | 通过 |
| 服务器包敏感内容 | 禁止文件 0，禁止标记 0 |
| Windows 发布脚本、客户包与更新定向安全测试 | Python 与 Node 定向测试全部通过 |
| 本机配置密钥与发布源文件比对 | 已比对 7 个本机供应商密钥值，命中 0；不输出密钥值 |
| 现有示例域名目录版 | 被正式包门禁正确拦截；没有生成新 EXE |
| 生产启动、ASR 独立开关、幂等恢复与备份定向回归 | 24 + 12 + 7 项通过；无真实供应商调用 |
| 隔离双进程真实页面 | 客户登录、服务器 100 积分、本机工作台、管理员只读数据、大树1形象/声音和收费项目折叠均通过；控制台无业务错误 |
| 单机客户绑定 | 首个客户可用；第二个客户返回 409；服务器未保留第二客户有效会话 |
| 后台任务登录门禁 | 转写、发布、智能创作在无当前绑定客户会话时不领取任务；11 + 4 项定向回归通过 |
| 双角色身份与退出 | 普通业务只使用客户身份、管理接口只使用管理员身份、管理员退出撤销上游会话；后端 19 项、前端客户端 8 项回归通过 |
| 前端依赖审计 | 生产依赖 0；开发依赖仅剩 Vite 本地开发服务器 1 高危 + 1 中危，客户包不携带 Vite 或 `node_modules` |

服务器结构核验包：

- 文件：`build/control-plane-audit-20260810-2/VideoInsight-control-plane-0.2.1-audit2.zip`
- SHA256：`f4232084bfc24bb256911ed37da487af81c69cbaf65401c4e19826abf32ee4b7`
- 说明：171 个条目、禁止运行文件 0、共享形象和声音各 1；这只是本机服务器包结构核验样本，不是正式服务器包或客户安装包，不要发送。

## 暂时搁置的事项

| 事项 | 为什么现在不能完成 | 恢复条件 | 放行标准 |
| --- | --- | --- | --- |
| 公司服务器正式部署 | 缺少正式域名、DNS 和服务器生产 `.env` | 提供公司 Linux 服务器登录方式和公司控制的域名/子域名 | `/health`、`/ready`、HTTPS、安全响应头和 `verify_live.sh` 全通过 |
| 普通客户与管理员跨电脑验收 | 本机隔离双进程已通过，但同一台电脑不能证明公网 HTTPS、真实网络和跨设备行为 | 正式 HTTPS 服务启动，创建低积分内部测试码 | 登录、余额、退出、禁用、会话过期和管理员隔离真实通过 |
| 最终 EXE 页面全量点击验收 | 本机源码双进程核心路径已通过，尚未在唯一最终安装包和干净电脑上逐页跑完 | 正式域名就绪后生成唯一最终 EXE | 普通用户和管理员核心页面无空白、死链或控制台业务错误 |
| “大树1”正式线上可见和选择 | 本机双进程页面已显示并自动选中，尚无公司公网服务器证据 | 正式服务器导入共享清单，普通客户登录 | 页面显示形象和声音均为可用，不出现重新训练或训练扣费 |
| AI 文案、云转写、云成片、数字人成片 | 身份路由与幂等已自动化通过，但真实调用供应商并可能收费 | 服务器填写合法密钥，负责人逐项批准一次最低成本测试 | 每项只提交一次，客户余额真实只扣一次，失败恢复符合规则 |
| 双角色登录、退出和会话过期实页复验 | 自动化已覆盖身份选择与撤销；本轮浏览器实页控制连接未建立 | 正式 HTTPS 服务和最终 EXE 就绪 | 普通业务按客户扣费，管理接口只认管理员；退出后旧会话立即失效 |
| 真实平台发布 | 需要专用平台账号、验证码和不可逆发布确认 | 准备测试账号并在操作前再次确认 | 平台真实返回发布结果，系统不把“已上传”误报为“已发布” |
| 最终正式安装包 | 正式服务器地址尚未确定；现有 0.2.0 是示例地址验证物 | 正式域名和无付费登录验收通过，并选择未使用的新版本号 | 只生成一个最终 EXE，核对 SHA256，并在干净电脑双击验收 |
| Windows 代码签名 | 尚未购买证书 | 公开销售前确定预算 | 签名有效；未签名内测必须如实提示 SmartScreen 风险 |
| 正式 Git 发布提交 | 当前工作区有 167 个变更条目（90 个已跟踪修改、77 个未跟踪文件），包含多轮功能改动和正式构建产物；暂存区为 0 | 确认本批全部属于本次正式版范围并审核提交说明 | 工作区干净后无参数运行 `check_release.ps1` 通过 |

## 需要准备的最少信息

正式部署时只需要准备以下内容，不要发到聊天或写进源码：

1. 公司 Linux 服务器的登录方式，以及是否已经安装 Docker。
2. 一个公司控制的域名或子域名，并能修改 DNS 解析。
3. 服务器上填写的管理员初始密码和供应商密钥。
4. 一个低积分内部测试激活码。
5. 后续每项真实收费测试的单独批准和预算上限。

## 恢复执行顺序

1. 上传唯一的公司控制层 ZIP，在服务器本地填写 `.env`。
2. 运行 `deploy/control-plane/deploy.sh` 和 `verify_live.sh`，先完成全部不收费验收。
3. 用普通客户和管理员账号从公司网络之外验证权限、余额和“大树1”。
4. 每个收费能力经批准后只做一条最低成本真实测试。
5. 所有证据完成后，使用未出现过的新版本号只运行一次 `build_final_windows_release.ps1`；脚本会先做在线无付费检查、完整发布门禁和包内密钥扫描。
6. 在干净 Windows 电脑双击验收后，只发送这一个最终安装包。

## 明确不要做

- 不发送任何 `avatarcheck` ZIP 给客户。
- 不继续发送旧的 0.1.x 安装包。
- 不发送当前示例域名的 0.2.0 目录版或安装包，也不覆盖同版本文件。
- 不把 `.env`、供应商密钥、数据库、日志、媒体或训练样本放进 EXE。
- 不为了“测试一下”重复提交转写、数字人、云剪辑或真实发布。
- 不在工作区仍有未审核改动时把门禁的 `-AllowDirty` 结果当作正式发布证据。
