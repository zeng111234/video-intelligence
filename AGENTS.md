# 项目协作与产品设计规则

## 面向小老板的极简产品规则

### 1. 默认用户画像

- 默认服务对象是：时间紧、耐心少、技术基础有限，不愿学习复杂软件；愿意为明确的业务结果付费，但对每一笔费用都非常敏感的小老板。
- 用户真正想要的是“帮我把事情办成”，而不是研究模型、参数、接口或工作流。产品和 Agent 应主动承担复杂度，只把必要的业务选择交给用户。
- 上述画像仅用于约束产品设计，不得在界面、文案或沟通中贬低用户，也不得利用用户不懂技术而诱导付费、隐瞒成本或夸大能力。

### 2. 默认采用“极简模式”

- 默认只展示一条推荐路径；高级参数、备用方案和技术细节收进“高级设置”，用户不打开也能完成核心任务。
- 一个页面只解决一个主要问题，并且只保留一个最醒目的主按钮。按钮直接写结果，例如“生成文案”“确认并制作视频”，不要使用“执行任务”“提交参数”等技术化表达。
- 核心流程从进入功能到提交任务原则上不超过 3 个步骤；每一步只要求填写当前不可缺少的信息。确实无法满足时，必须说明增加步骤的业务理由。
- 整条流程可以在同一工作台连续完成，但默认只展开当前步骤，已完成步骤自动收起并显示结果摘要；不要像复杂控制台一样在首屏并排堆满所有模块。
- 每一步都要清楚回答三件事：现在要做什么、系统会帮我做什么、完成后能得到什么。
- 页面必须使用日常中文。除非用户主动查看详情，否则不展示模型名、接口名、状态码、Token、JSON、路径或其他技术术语。

### 3. 少填、少选、少重复

- 能自动推断的就自动推断，能提供可靠默认值的就默认选好，能复用历史内容的就自动带入；不得为了“看起来专业”增加无意义表单。
- 用户填写过一次的信息、确认过的素材和上一步生成的结果必须自动流转到后续环节，不得要求跨页面复制粘贴或重复选择。
- 账号、密钥、品牌资料、默认人物和发布偏好等内容只在配置页设置一次；日常操作页直接复用。缺少配置时，给出直达配置入口和简短说明。
- 多条内容采用批量导入、队列处理、批量确认和统一状态查看；不要让用户逐条进入多个页面重复操作。
- 自动保存草稿和当前进度。刷新、返回或任务失败后，用户已经填写和生成的内容不得丢失。

### 4. 自动化但不越权

- 可以自动完成提取、生成、排版、转交和预检查，但涉及事实准确性、内容改写、付费调用、账号授权、真实发布、覆盖或删除时，必须保留明确的人工确认。
- 确认后的内容应自动带入下一步，并提供一个清晰的“继续完成后续流程”按钮；不要让用户自己寻找下一页面。
- 不能因为界面写着“一键完成”就伪造结果。第三方能力不可用、平台不支持自动发布或任务仍在处理中时，必须如实显示，并提供最简单的可行替代方案。
- 系统应明确区分“已创建任务”“处理中”“等待确认”“已生成”“已上传”和“平台已发布”，不得把中间状态包装成最终成功。

### 5. 抠门用户的成本规则

- 默认选择能够满足目标的低成本方案，优先复用已有结果、缓存和已购买资产，避免无意义的重复生成和重复付费调用。
- 在任何可能产生费用的动作之前，用人民币或用户可理解的计费单位说明预计费用或费用范围、可能耗时、是否由第三方收费；不能确定时必须写“暂无法确定”，不得猜测。
- 对重复生成、批量处理和可能持续计费的任务，先显示数量与预计总成本，并让用户确认；不得静默发起高成本任务。
- 提供预算上限或低成本选项时，默认推荐最省钱且能交付可用结果的方案，同时说明它相对高价方案少了什么。
- 失败是否扣费、能否重试、是否需要重新付费必须讲清楚；不得把沉没成本隐藏在模糊状态里。

### 6. 出错时让用户马上会处理

- 错误提示先用一句人话说明“发生了什么”，再给一个首选解决按钮，例如“重新连接”“补充配置”“保留内容并重试”。
- 不把原始报错、堆栈或状态码直接扔给普通用户；技术详情可以折叠展示，供排查时复制。
- 失败后必须保留用户输入，提供可恢复路径；如果自动方案不可用，应给出最少步骤的手动方案。
- 网络、API 或工具因连接问题失败时最多自动重试一次；仍失败就停止并清楚报告，不得让用户等待无休止的重连。

### 7. 每次产品改动的验收门槛

- 5 秒可懂：第一次打开页面，用户能迅速看懂这个页面能做什么，以及应该点击哪里开始。
- 无需培训：不看说明书、不懂技术的新用户，可以独立提交一次核心任务。
- 主路径最短：默认路径没有非必要字段、重复输入、重复确认或跨页面复制粘贴。
- 状态可信：用户随时知道当前进度、下一步、预计费用，以及结果是否真的完成。
- 操作可恢复：除明确确认的不可逆操作外，用户可以返回、修改、取消或继续，且已有内容不丢失。
- 页面真实验收：涉及前端流程的改动必须在实际运行页面中验证核心路径；仅通过构建、类型检查或单元测试不能算完成。

## 用户偏好

- 当网络、API 或工具调用因连接或重连问题失败时，最多自动重试一次；仍失败则清楚报告，不要循环重连。
- 除非用户明确要求弹性重试，不得连续执行五次重连或重试。

## 已知陷阱 / 硬约束

### 抓取抖音 / 小红书 / B站必须保留反检测脚本

`src/adapters/drission_browser.py` 中的 `ANTI_DETECTION_INIT_SCRIPT` 与
`--disable-blink-features=AutomationControlled`、`--disable-infobars` 启动参数，
以及 `src/adapters/platform_browser_search.py` 中的
`context.add_init_script(ANTI_DETECTION_INIT_SCRIPT)` 调用，**任何一项缺失
都会立刻被抖音 / 小红书 / B站风控识别为自动化**，导致搜索页要求扫码登录或
直接拦截。表现是 platform crawl 返回 0 候选 / `link-transcriptions` 报
"该分享页没有返回可转写的视频流"，下游 production pipeline 因此跑不起来。

历史教训：dirty 改动曾误删以上 4 处反检测，结果"抓取"环节静默 0 结果；
ASR 本身（阿里云 fun-asr）其实工作正常，被错误归因。下次任何人重写
这两个文件前必须先确认反检测完整保留。

### ASR 失败不要把原始异常压成统一文案

`src/services/transcription.py:705-721` 的"阿里云语音识别失败，素材已保留；
不会使用本地 CPU"统一包装会把阿里云 fun-asr submit/query 的真实错误吞掉，
导致 `task_id` 为 null、系统"素材不可恢复不会自动重试"。前端应展示
`exc.__cause__` 的代表性字段（HTTP 状态、API 错误码），并把 retry 按钮
文案改为"重新上传并识别"，避免用户陷入死循环。

### work/ 目录下的脚本不作为产品验收

`work/` 下的拼接脚本（ffmpeg concat、手工 timeline 拼装、PIL 数据卡生成）
只允许做**失败证据**或**本地预览**，**不得作为产品验收的最终 MP4**。
产品验收必须满足以下全部条件：

1. 通过正式 `/video-editor` 页面（http://localhost:1001/video-editor）触发
   并完成 quote → user_confirm → generation → export 完整链路；
2. 最终 MP4 包含 **H.264 视频轨 + AAC 音频轨 + 烧录字幕**（ass 来自
   `subtitle_manifest` 不可变 cue manifest，sha256 校验通过）；
3. 同时输出 `director_timeline`、`provider_search_log`、
   `image_generation_log`、`quality_report` 与 `contact_sheet`；
4. 实际页面播放 + 下载 200 证据齐全；
5. 真人观看确认：声音与字幕一致、字幕动效可见但不乱、至少 1 个全屏
   B-roll、至少 1 个 PiP 或局部视觉强调、不出现深色万能卡、不出现
   遮脸 / 裁头 / 黑边 / 遮字幕、不出现与场景无关的外国素材。

`work/director-plan-20260831/trial-30s/trial-30s-v3.mp4` 这类手工拼接产物
可以作为阶段性证据保留在 `work/` 树里，但**禁止继续修补或纳入正式流程**——
任何"修改 trial-30s 脚本让它更接近正式"的尝试都是在向错误方向努力。

## 提示词优化与专家团

- 当用户说“优化提示词”“优化提示词并执行”“启动优化提示词”“专家团”“智能路由”，或显式调用 `$prompt-expert-team` 时，加载并遵循全局 `prompt-expert-team` Skill。
- 当用户说“只优化”“不要执行”或“先给我看优化结果”时，仅整理请求并展示专家路由预览；不启动 Agent，也不改变状态。
- 否则，调用该工作流即表示：保留原始意图，整理 Task Contract，选择最小充分专家团队，按 Skill 委派，等待必要阶段，完成验证，并返回一份汇总结果。
- 本规则只为上述工作流显式请求子代理委派；普通任务若未触发这些条件，也没有其他委派需求时，不启动专家团。
