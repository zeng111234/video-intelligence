# VideoInsight 交付前本地真实验收与剩余事项（2026-08-10）

## 结论

不依赖正式服务器和域名的本地核心链路已经完成本轮验收：普通客户真实登录、工作台筛选、抖音登录搜索、AI 文案、云端转写、云剪辑、使用现有“大树1”生成数字人成片、积分幂等、激活码防暴力、前后端测试、构建以及 Windows 发布安全门禁均已验证。

当前可以进入“等待公司服务器 SSH 放行并配置 HTTPS”的阶段，但还不能称为最终商用交付版。本地改动已整理为提交 `76c4691`，提交后的干净工作区发布门禁已经通过；服务器部署包 `VideoInsight-control-plane-0.2.3.zip` 已生成。本轮没有训练形象、没有克隆声音、没有上传训练样本、没有真实发布到任何平台，也没有生成新的最终 EXE。

## 1. 登录态与抖音采集

- 抖音专用浏览器资料目录为 `data/browser_profiles/douyin`，本轮检查结果为已登录、无需重新登录、可以采集。
- 实际请求使用专用登录浏览器，不是匿名网页。代码和界面中容易误导的“公开页面/公开搜索”已经统一改为“抖音登录搜索”，对外模式改为 `local_browser`。
- 普通客户工作台真实请求只选择抖音，并真实提交了时间条件、每平台数量 30、禁止收费回退等筛选参数；截图保存在本机临时验收目录。
- 本次“一天内、30 条”搜索得到 1 条视觉参考素材。随后尝试立即做不限时间的 30 条对照时，被同平台至少间隔 60 分钟的冷却保护拒绝，没有绕过保护、没有重复采集、没有扣积分。
- 因此，“登录态和筛选条件是否真实生效”已经通过；“冷却结束后不限时间是否稳定返回更多条”仍需到时复验，不能把当前 1 条归因成单列/多列布局。单列/多列只影响展示，不决定后端采集数量。
- 点赞、评论、分享/转发、收藏等指标会在平台实际返回时展示；平台未返回的字段必须明确显示为未返回，不能伪造为 0，也不能承诺所有视频都能拿到完整指标。

页面证据：

- `C:\Users\zeng\AppData\Local\Temp\VideoInsight-paid-acceptance-20260810\pipeline-login-page.png`
- `C:\Users\zeng\AppData\Local\Temp\VideoInsight-paid-acceptance-20260810\pipeline-filter-open.png`
- `C:\Users\zeng\AppData\Local\Temp\VideoInsight-paid-acceptance-20260810\pipeline-crawler-results.png`

## 2. 最低成本真实供应商验收

所有收费请求均使用唯一幂等键；相同请求重复确认只返回原任务，不再次扣分。下表记录的验收发生在数字人按秒计费修复之前：当时初始测试余额 20.00，最终余额 18.08，总消耗 1.92 积分，其中数字人因错误沿用固定 45 秒报价而多扣 1.79 积分。该记录仅用于说明历史事实，不能作为当前计费验收结论；按当前规则复算，同一组操作应消耗 0.13 积分、余额应为 19.87。

| 能力 | 真实结果 | 费用与幂等证据 |
| --- | --- | --- |
| AI 文案 | DeepSeek 成功生成，469 tokens，任务 `copy-d68b6a44a4`，非模拟 | 扣 0.01；相同幂等键返回同一任务，只扣一次 |
| 云端转写（无语音样本） | 音调测试文件被供应商明确判定无可识别语音，没有自动重提 | 扣 0.01；记录失败原因并停止 |
| 云端转写（有效中文语音） | 成功得到两句中文，任务 `transcript-a14f519f55`，供应商任务 `cee59b30-397f-487c-b4f2-a31f15ea0871` | 扣 0.01；只授权、上传、提交一次 |
| 云剪辑 | 任务 `edit-item-228ae9ad8dbf` 成功恢复并下载真实 MP4，H.264/AAC、360×640、8.22 秒 | 扣 0.01；恢复结果未重新渲染、未重复扣分 |
| 大树1数字人 | 使用已训练的形象 `shuying-avatar-21920` 和声音 `shuying-voice-7869`，任务 `avatar-2a75216ba57e`、供应商视频任务 45998 成功 | 历史版本错误按固定 45 秒扣 1.88；成片实际 1.067 秒，当前规则向上取整为 2 秒，应结算 0.09，旧测试账户多扣 1.79；相同幂等键仍只结算一次 |

按秒计费修复后的无付费复验：14:52 重启的新后端 `/health` 正常；用“你好”调用报价接口返回 2 秒、0.09 积分；后端计费专项 55 项、前端报价与提交边界 22 项全部通过。这里只读取报价，没有再次提交数字人任务或产生供应商费用。

真实媒体证据：

- `C:\Users\zeng\AppData\Local\Temp\VideoInsight-paid-acceptance-20260810\desktop\data\avatar_results\avatar-2a75216ba57e.mp4`：575,081 bytes，H.264/AAC，540×960，1.067 秒。
- `C:\Users\zeng\AppData\Local\Temp\VideoInsight-paid-acceptance-20260810\desktop\data\video_edits\cloud_results\edit-cloud-228ae9ad8dbf.mp4`：90,100 bytes，H.264/AAC，360×640，8.22 秒。

## 3. 普通客户工作台与管理员边界

- 本轮不是只用管理员页面。隔离控制层、桌面 API 和前端启动后，已通过实际登录页以普通客户身份进入工作台，余额显示 18.08，筛选弹层和素材结果页均由普通客户页面完成。
- 正常运行的 1001/2001 已于 14:52 使用当前代码重启，进程和 `/health` 均已核对；旧路由不再是当前运行证据。本轮浏览器控制只能打开一个新的未登录会话，不能复用用户已登录标签，因此没有擅自填写激活码，也没有把当前 1001 的普通客户按钮点击写成已通过；普通客户实页仍沿用此前隔离实例证据，并保留到最终 EXE 再验。
- 管理员和普通客户身份路由、退出和媒体 Cookie 隔离已有自动化覆盖；普通业务不能借管理员身份绕过客户扣分。

## 4. 激活码防暴力与客户隔离

- 新激活码使用约 80 bit 的安全随机数，不是可预测的递增编号。
- 单 IP 登录限速 10 次/分钟，全局 300 次/分钟；同一目标失败 5 次后锁定 15 分钟。
- 不存在、已禁用和错误激活码返回同一种提示，减少有效码枚举；伪造 `X-Forwarded-For` 不能绕过本机限速。
- 会话令牌使用 32-byte 安全随机值并以摘要持久化；普通客户不能访问管理接口，同一本机数据目录不能悄悄切换到另一个客户。
- 新码默认套餐为 7 天 / 9.9 积分，7 天从首次成功登录开始计算；9.9 是管理员记录的使用套餐售价，不会从客户的内容积分余额中再次扣除。
- 未首次登录时调整套餐不会提前开始倒计时；已激活码续期从当前未来到期日继续累加，已过期码从续期时重新起算。续期会撤销旧会话，但不会清零或增减内容积分。
- 有限期会话令牌不会晚于套餐到期时间；到期后拒绝继续登录并提示续期，客户原有数据和剩余内容积分保留。旧版激活码迁移后仍按长期有效处理，不会突然过期。
- 这些措施能防止实际可行的循环穷举，但不能防止客户主动把正确激活码转发给别人。正式服务器阶段仍需增加设备绑定、设备列表、主动下线和换机审核。

## 5. 自动化与 Windows 门禁

| 检查 | 当前结果 |
| --- | --- |
| Python 完整测试 | FastAPI/控制层完整套件、根服务与页面隔离分组全部通过，无失败；续期边界另有定向回归 |
| 数字人按秒计费专项 | 后端 55 项、前端 22 项通过；短文案报价 2 秒 / 0.09 积分 |
| React/Vitest | 21 个文件、159 项通过；包含限时激活套餐的管理员展示和默认值测试 |
| TypeScript | `npx tsc --noEmit` 通过 |
| 前端正式构建 | `npm run build` 通过；仅保留主包大于 500 kB 的非阻塞警告 |
| 抖音/工作台定向回归 | 67 项通过 |
| 激活码、会话、控制层和收费幂等定向回归 | 27 项通过 |
| Windows 发布、客户包和密钥扫描定向回归 | 16 项通过 |
| 桌面自动更新安全测试 | 7 项通过 |
| PowerShell 脚本语法 | `scripts/*.ps1` 全部通过 |
| 构建机密钥与发布源码比对 | 检查 7 个本机供应商密钥值，命中 0，不输出密钥值 |
| Git 与发布门禁 | 改动已提交为 `76c4691`；提交后工作区干净，`scripts/check_release.ps1` 全量通过 |

本轮没有运行 `build_final_windows_release.ps1`。该脚本会生成最终安装包和更新清单，必须等正式 HTTPS 公司服务地址确定、工作区范围审核完成后再使用全新版本号运行一次。

## 6. 暂时搁置的事项

| 事项 | 当前不能完成的原因 | 放行条件 |
| --- | --- | --- |
| 抖音不限时间 30 条即时对照 | 同平台 60 分钟冷却保护尚未结束 | 冷却结束后只做一次免费搜索，核对返回数、时间回执和互动指标来源 |
| 公司控制层正式部署 | 已有 2 核 4G 共享服务器和域名 `xmt.syszr.cn`；DNS 已指向 `47.121.114.248`，但默认 SSH 22 端口两次连接超时，HTTP 仍是其他服务的 404，HTTPS 尚未完成握手 | 先在安全组仅向运维来源开放实际 SSH 端口，或提供非 22 端口；连接后先只读检查现有服务、反向代理、端口和目录，再隔离部署 |
| 设备绑定和换机审核 | 需要公司服务器作为唯一授权中心，并确定一码允许几台设备 | 设备登记、主动下线、换机审核和管理员记录通过 |
| 跨电脑普通客户验收 | 当前真实页面证据来自同机隔离双进程，正式 HTTPS 尚未就绪 | 在 `https://xmt.syszr.cn` 上用低积分测试码验证登录、余额、禁用、退出、套餐到期和会话失效 |
| 唯一最终 EXE | 服务地址已经确定，但服务器尚未部署并通过 HTTPS 在线门禁 | 服务器在线检查通过后，用未使用版本号只生成一次并做干净电脑验收，避免再次浪费传输流量 |
| Windows 代码签名 | 尚未购买签名证书 | 公开销售前确定预算；未签名内测需如实说明 SmartScreen 风险 |
| 真实平台发布 | 属于不可逆外部动作，且本轮明确禁止 | 准备专用测试账号并再次明确批准后，只发布一条并核对平台真实结果 |

## 7. 服务器和域名到位后的最短顺序

1. 在云安全组仅向运维来源开放实际 SSH 端口；若不是 22，提供实际端口。无需重新发送密码。
2. 登录后先做只读盘点，确认现有反向代理、容器、监听端口和磁盘目录。服务器上已有另一个系统，禁止覆盖、移动或删除其他域名、站点、容器、数据库和文件。
3. 只为 `xmt.syszr.cn` 增加独立站点规则和 VideoInsight 独立目录、容器、数据库卷；在服务器本地填写生产 `.env`，不得把密钥写进仓库或部署包。
4. 先跑 HTTPS `/health`、`/ready`、安全头、备份恢复和无付费在线检查，再创建一个 7 天 / 9.9 积分的低内容余额内部测试码，验证首次登录、到期时间、续期、禁用、退出和积分不误扣。
5. 导入部署包内已有的“大树1”形象和声音编号，确认页面显示“已训练完成”，禁止提交重新训练或克隆。
6. 收费能力只在生产配置确有差异时各做一次最低成本复验；数字人使用“大树1”，真实发布仍需再次明确批准。
7. 服务器在线门禁全部通过后，使用全新版本号生成唯一最终 EXE，在干净 Windows 电脑双击安装、登录、更新和卸载验收后，只发送这一个文件。

当前待部署服务器包：

- 路径：`build/control-plane-ready-0.2.3/VideoInsight-control-plane-0.2.3.zip`
- SHA256：`b832315370dfe253529f045949a3655de9cf6567dc6db301761a3731ca7ee43e`
- 共 175 个 ZIP 条目；已核对敏感/运行文件命中 0，包含迁移 011 和共享“大树1”资产清单。

## 明确禁止

- 不训练新形象，不克隆新声音，不上传训练样本。
- 不把 `.env`、供应商密钥、数据库、日志或客户媒体打进 EXE。
- 不在结果未知时自动重复提交收费任务。
- 不为了绕过冷却而连续重启浏览器或反复请求抖音。
- 不在正式服务器和不可逆确认未完成时真实发布。
