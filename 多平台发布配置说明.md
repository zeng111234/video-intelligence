# 多平台发布配置

实际生效文件是同目录的 `.env`。请将下面三项填入 `.env`，不要把真实密钥发到聊天或提交到 Git：

```env
PUBLISH_DOUYIN_CLIENT_KEY=
PUBLISH_DOUYIN_CLIENT_SECRET=
PUBLISH_DOUYIN_REDIRECT_URI=
```

`PUBLISH_DOUYIN_REDIRECT_URI` 必须是抖音开放平台已登记的 HTTPS 地址，并指向：

```text
https://你的域名/api/v1/publish/connections/douyin/callback
```

保存后，重启后端，进入“多平台发布”并点击“扫码连接抖音”。扫码获得的账号授权信息由系统自动保存到 `.env`，不需要手填 Token。

快手、视频号、小红书的 Key/Secret 模板也已放入 `.env.example`，但当前版本尚未实现它们的官方扫码适配器，仍使用人工发布助手。
